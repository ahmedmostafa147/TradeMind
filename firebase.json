{
  "//": "Hosting used to live here. The site moved to Vercel, which serves it as a real Next build instead of a static export — see site/vercel.json and the `output: 'export'` note in site/next.config.ts. The block was deleted rather than left unused: with it in place a stray `firebase deploy` republishes a second, stale copy of the whole site — legal pages included — at trademind-6222c.web.app, and two live copies of a published privacy policy is a real problem. This file now exists only to deploy Firestore rules.",

  "firestore": {
    "rules": "firestore.rules"
  }
}
