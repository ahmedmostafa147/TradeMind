# Radar — رادار

دفتر صفقات البورصة المصرية. تطبيق أندرويد (Flutter) + موقع وداشبورد (Next.js).
الواجهة عربي RTL بالكامل. الاتنين مجانيين حاليًا.

> هذا الملف بيتحمّل تلقائيًا في أي محادثة جديدة. حدّثه لما حاجة جوهرية تتغيّر.
> آخر تحديث: 4 أغسطس 2026.

---

## الحالة الحالية

| | |
|---|---|
| الفرع | `main` — كل الشغل اتدمج فيه، ومفيش فروع تانية |
| الاختبارات | `flutter analyze` نضيف · `tsc` نضيف |
| النشر | **الموقع نازل** على <https://radar-one-phi.vercel.app> (مشروع Vercel `radar`) · قواعد Firestore منشورة · التطبيق لسه مانزلش على Play |
| مشروع Firebase | `trademind-6222c` (اسم العرض Radar) — مربوط في `.firebaserc` فمش محتاج `--project`. بقى للـ Auth وFirestore بس |

الاسم اتغيّر من TradePilot لـ **Radar** في كوميت `f776f16`. اسم الحزمة الداخلي في
`pubspec.yaml` لسه `egx_trade_journal` — ده اسم داخلي مالوش أثر على المستخدم،
وتغييره بيلمس كل ملف import في المشروع، فاتساب عن قصد.

---

## البنية

```
lib/                    تطبيق Flutter
  core/calc/            منطق حسابي بحت — صفر Flutter imports (ده قيد صارم)
  core/theme/           الثيم، والباليتة مولّدة (متعدّلهاش بإيدك)
  features/auth/        Firebase Auth — تسجيل الدخول إجباري
  features/sync/        مزامنة Firestore + كتابة ملف تعريف المستخدم
  features/market/      أسعار الإغلاق (Yahoo endpoint غير رسمي)
  features/ai_parser/   قراءة توصية من صورة بـ Gemini (بمفتاح المستخدم)
  trades/ watchlist/ today/ dashboard/ calculator/ settings/ shell/

site/                   Next.js 16 + Tailwind 4 — على Vercel، مش تصدير ثابت
  app/(marketing)/      الصفحة الرئيسية + الصفحات القانونية (بهيدر وفوتر)
  app/(app)/            /dashboard و /admin (شل بسيط، من غير الهيدر التسويقي)
  lib/                  firebase, auth-context, trade, risk-math, format, site
                        + account-settings.ts (قاعدة المخاطرة من الحساب)
  components/dashboard/ الداشبورد الاتنين + بانل تسجيل الدخول + محرّر التايم لاين
  app/manifest.ts       PWA manifest (راوت، مش ملف ثابت)
  public/sw.js          service worker — أوفلاين + شرط التثبيت
  components/pwa.tsx    التسجيل وزرار «ثبّت التطبيق»

design/palettes.json    مصدر الألوان الوحيد للتطبيق والموقع
tool/gen-theme.mjs      بيولّد باليتة Dart + توكنز CSS من الملف اللي فوق
firestore.rules         قواعد الأمان — فيها نموذج الأدمن
```

---

## قواعد لازم تتحط في الاعتبار قبل أي تعديل

### ١. الألوان ليها مصدر واحد — متعدّلش الملفات المولّدة

كل لون بيتولّد من `design/palettes.json`:

```bash
npm --prefix site run theme
```

بيكتب `lib/core/theme/palettes/generated_palettes.dart` و `site/app/tokens.css`،
والاتنين عليهم `DO NOT EDIT`. و`npm run build` بيرفض يكمّل لو اتفرّقوا عن المصدر.

**تلات قواعد مفروضة في المولّد نفسه:**

- **ألوان الفلوس محميّة.** `win` و`loss` و`breakeven` في بلوك `semantics` بره
  الباليتات، وأي باليتة تحاول تعيد تعريفهم المولّد بيرفض يشتغل. السبب: المتداول
  بيتعلّم إن الأخضر = ربح؛ لو تغيير الثيم قادر يعيد صبغه، اللون يفقد معناه.
- **بوابة تباين.** 30 زوج بيتقاسوا في كل باليتة قبل أي كتابة. الرسوب بيوقف
  التوليد.
- **`brand` خلفية مش نص.** الباليتة النشطة (`pharaoh`) لونها ليمون `#D9FF30`،
  تباينه **1.15:1** على الأبيض. النص الملوّن بالبراند بياخد `--brand-ink`
  (وهو `accent`)، والزراير بس هي اللي بتستخدم `bg-brand`.
  في Flutter: `ColorScheme.primary` مربوط بـ `accent` **مش** `brand` — لأن
  Material بيستخدم `primary` كخلفية وكنص في نفس الوقت.

**فخ متكرر**: `inverse-surface` **بينقلب مع الثيم** (فحمي في الفاتح، كريمي في
الداكن). ممنوع تحط `text-brand` جوّاه — بيطلع 1.01:1 في الوضع الداكن. استخدم
`text-on-inverse-surface`، والليمون يدخل كـ**بادج مملوء** بنص غامق.

### ٢. الأرقام: إبسيلونات، وأرقام غربية

`lib/core/calc/risk_math.dart` فيه `kQtyEpsilon` و`kRiskEpsilon`، والاتنين
**مرتبطين ببعض**. `exceedsRiskLimit()` هي **المكان الوحيد** اللي ينفع تتقارن فيه
نسبة مخاطرة — متكتبش `riskPct > maxRiskPercent` في أي مكان تاني.

كل الـ formatters مبنية على locale `'en'` **عن قصد** — locale `'ar'` بيطلّع أرقام
عربية هندية. و`parseNumber` بيقبل `٠-٩` لأن الكيبورد العربي بيطلّعها.
`site/lib/risk-math.ts` و `site/lib/format.ts` نسخ مطابقة للنسخة الـ Dart.

### ٣. الصفحات القانونية جزء من الكود

`site/app/(marketing)/privacy/page.tsx` و `terms/page.tsx` **مستندات قانونية
منشورة**، ومراجع Play بيقراها. أي تغيير في سلوك التطبيق بيلمس بيانات المستخدم
لازم يتحدّث فيها **في نفس الكوميت**.

سبق وحصل: لما تسجيل الدخول بقى إجباري، كان فيه **٥ أماكن** بتقول «شغّال من غير
حساب» — واحدة منهم في سياسة الخصوصية وواحدة في الشروط.

كمان: `RELEASE.md` بيقول إن الوصف لازم يقول صراحةً إن التطبيق **مش نصائح
استثمارية** عشان Play مايصنّفوش تحت الفئات المالية المقيّدة. الجملة دي في
`site/lib/site.ts` باسم `disclaimer` وبتظهر في الفوتر والشروط.

### ٤. الأمان — نموذج الأدمن

الأدمن = وجود مستند في مجموعة `admins`، و**مفيش أي مسار عميل يقدر يكتب فيها**
(`allow write: if false`). بتتعمل من Firebase Console بس.

الفلاج `isAdmin` في React **بيقرر الشكل بس**. كل استعلام وراه بيتفحص تاني على
السيرفر.

**الأدمن مش بيقرا صفقات أي حد** — بيشوف ملفات التعريف والعدّادات بس. ده مقصود:
سياسة الخصوصية بتقول إن قواعد السيرفر بتمنع أي مستخدم تاني من قراءتها. لو حد
طلب تغيير ده، لازم السياسة تتغيّر وتتعاد إتاحتها **الأول**.

**وعشان كده `capital` عايش في مجموعة فرعية.** إعدادات المخاطرة في
`users/{uid}/settings/risk` مش كحقول على ملف التعريف — لأن ملف التعريف هو
المستند الوحيد اللي الأدمن مسموح له يقراه، ورأس المال ده حجم محفظة المستخدم.
منح القراءة على مستند مبيوصلش لمجموعاته الفرعية، فالقاعدة هنا هي نفس قاعدة
الصفقات: المالك وبس.

### ٥. الويب بيكتب في Firestore — والتطبيق بيقرا اللي بيكتبه

`site/lib/trade.ts` و`watchlist.ts` فيهم `encodeTrade`/`encodeWatchlistItem`،
ولازم يفضلوا مطابقين لـ `SyncCodec` بالحرف. **أربع قواعد لو اتكسرت البيانات
بتتلف بصمت:**

1. **التواريخ نصوص ISO مش Timestamps.** الـ codec في Dart بيعمل
   `value is String ? DateTime.tryParse(value) : null` — تديله Timestamp
   يبقى التاريخ `null`، ولـ`entryDate` ده معناه إن كل صفقة من الويب تاريخها
   يقفز لـ«دلوقتي» على التليفون.
2. **مفتاح المستند = `trade.id`**، والكتابة `merge: true`. غير كده نفس الصفقة
   بتتفرّع لمستندين.
3. **`screenshotPaths` بيتقرا وما بيتكتبش أبدًا.** كتابته فاضية مع الـ merge
   بتمسح صور الشارت اللي التليفون كتبها — من واجهة أصلاً مش بتعرضها.
4. **`timeline` بيتكتب، بس بالمصفوفة كاملة.** Firestore بيستبدل حقل المصفوفة
   مش بيدمج عناصره، فأي حفظ لازم يبدأ من القايمة المخزّنة — الفورم بيتعبّى من
   الصفقة المحمّلة عشان كده بالظبط. (كان محذوف زي `screenshotPaths` لحد ما
   بقى فيه محرّر تايم لاين في `site/components/dashboard/timeline-editor.tsx`؛
   حذفه كان بيخلي «آخر لمسة» = تاريخ الدخول دايمًا، و«محتاجة ملاحظة» تشتغل على
   صفقات التطبيق شايفها متابَعة.)

الحسابات كلها (`analytics.ts`، `risk-score.ts`، `decisions.ts`، `checklist.ts`)
نسخ مطابقة من `lib/core/calc/`. **ومفيش أي اختبار على نسخ الويب** — مفيش
`test` script في `site/package.json` ولا ملف اختبار واحد في `site/`. الحارس
الوحيد هو اختبارات Dart المقابلة (`risk_score_test.dart`،
`daily_decisions_test.dart`، `journal_analytics_test.dart`). يعني لو غيّرت
معادلة في Dart، الاختبارات هتمسكها هناك بس، ونسخة الويب هتفضل غلط بصمت لحد ما
حد يبص عليها بعينه. **غيّرها في الاتنين في نفس الكوميت.**

### ٦. حذف الحساب — عليه اختبار حارس

`test/account_deletion_test.dart` بيقرا سورس `auth_repository.dart` ويتأكد إن
`deleteAccount` بيمسح **`users/{uid}` كمان** مش الصفقات بس، و**قبل** ما يمسح
الهوية. السبب: سياسة الخصوصية (بند ٦) وصفحة `/delete` بيوعدوا بمسح «حساب الدخول
(البريد والاسم المعروض)»، وPlay بيطلب نفس الحاجة. والمستند ده بعد مسح الهوية
**بيبقى مستحيل الوصول له للأبد** — القواعد بتديه لصاحبه بس، وصاحبه اتمسح.

الاختبار بيقرا الملف مش بينده الدالة، لأن كل الدوال دي بتقف عند
`Firebase.apps.isNotEmpty` وبترجع من غير ما تعمل حاجة تحت `flutter test` — يعني
اختبار سلوكي كان هيعدّي سواء الكود موجود ولا لأ.

### ٧. `lib/core/calc/` ممنوع فيه Flutter imports

قيد صارم — ده اللي بيخلي كل معادلة تتختبر كـ unit test من غير widget binding.

### ٨. الـ PWA — الزرار مبيظهرش من غير الـ service worker

الموقع بيتثبّت كتطبيق. **`public/sw.js` مش رفاهية أوفلاين** — Chrome مبيطلّعش
حدث `beforeinstallprompt` (اللي زرار «ثبّت التطبيق» في `components/pwa.tsx`
مستنيه) غير لما يكون فيه service worker **مسجّل وفيه `fetch` handler**، بالإضافة
لـ manifest فيه `name`/`short_name`/`start_url`/`display: standalone` وأيقونتين
192 و512. لو واحد ناقص، الزرار مبيظهرش **ومفيش أي رسالة خطأ في أي مكان**.

تلات حاجات لو اتغيّرت تكسر حاجة صامتة:

1. **المستندات network-first، مش cache-first.** `/privacy` و`/terms` مستندات
   قانونية منشورة، وworker يقدّم نسخة قديمة منها لمستخدم أونلاين ده مش تفصيلة.
   الكاش بيتقري بس لما الشبكة تفشل فعلًا.
2. **أي حاجة cross-origin مبتتلمسش.** Firestore وIdentity Toolkit بيعدّوا على
   `fetch` — الـ worker بيرجع من غير `respondWith` خالص، فالمتصفح بيتصرّف كأنه
   مش موجود. صفقة قديمة من الكاش أسوأ بكتير من خطأ أوفلاين.
3. **`cache.put` جوّه `event.waitUntil`.** كتابة مش متنتظرة بتموت لو المتصفح
   وقّف الـ worker بعد `respondWith` — والفشل ده غير مرئي تمامًا: الصفحة شغّالة
   والكاش بس أفضى من المفروض. ده حصل فعلًا واتصلّح.

الأيقونات مولّدة بـ `npm --prefix site run icons` من `assets/logo.png`.
**مش مربوطة بالـ build** بعكس الثيم — الشرح في `site/package.json`.
والـ maskable ملف منفصل عن قصد؛ السبب مشروح في `site/README.md`.

---

## حاجات اتقررت وممنوع ترجع من غير سبب

| القرار | السبب |
|---|---|
| تسجيل دخول **إجباري** | قرار المالك (29 يوليو). الجلسة متخزّنة في Hive، فأول تشغيل بس محتاج نت — بعدها بيفتح أوفلاين |
| **Hive باقي** | مش «ميزة أوفلاين» — ده مخزن بيانات التطبيق. شيله = كل شاشة نداء شبكة |
| **مفيش باقات مدفوعة** | المالك قرر يفضل مجاني لحد ما يبقى فيه عملاء كتير |
| **مفيش ترشيحات أسهم مدفوعة** | تعارض مع `RELEASE.md` وخطر ترخيص FRA. اتشالت من الخطة |
| `playStoreUrl: null` | التطبيق مانزلش. كل أزرار التحميل بتتحوّل لـ«قريبًا» لوحدها |
| `userCount: null` | مفيش مستخدمين. السطر مختفي بالكامل لحد ما يبقى فيه رقم حقيقي |
| **صفر إيموجي** في الموقع | تفضيل صريح من المالك — أيقونات SVG بس (`site/components/icons.tsx`) |

---

## الخطوات المعلّقة على المالك

مرتّبة بالأهم:

1. **اختار دومين حقيقي.** الموقع شغّال على دومين `.vercel.app`، وده اللي كل
   رابط أساسي وخريطة الموقع وصورة المشاركة بيشاوروا عليه. لما تشتري دومين:
   ضيفه في Vercel → Domains، غيّر `NEXT_PUBLIC_SITE_URL` في Environment
   Variables، وأعد النشر — **وضيفه كمان في Firebase Console → Authentication
   → Settings → Authorized domains**، وإلا تسجيل الدخول بيرجع
   `auth/unauthorized-domain`.
2. **خطوات Play** — كلها في `RELEASE.md`: مفتاح التوقيع، سياسة الخصوصية
   المستضافة (بقى ليها رابط حقيقي دلوقتي)، نموذج Data Safety، وSHA-1 بتاع Play
   في Firebase.
3. **إعلان داخل التطبيق عن تحديث سياسة الخصوصية.** بند ٩ في السياسة بيوعد
   بتنبيه قبل ما أي تغيير جوهري يسري، وتحديث ٤ أغسطس أضاف إعدادات المخاطرة
   لجدول البيانات المخزّنة. فيه تبويب «المستجدات» جاهز للغرض ده.

**اتعمل خلاص:** قواعد Firestore منشورة · حساب الأدمن اتعمل · الموقع نازل على
Vercel.

### النشر

**مفيش أمر.** المستودع موصول بمشروع Vercel `radar`، فأي `git push` على `main`
بينشر لوحده.

تلات إعدادات على المشروع هما اللي بيخلّوه يشتغل، ولو واحد اتغيّر البناء بيقع:

| الإعداد | القيمة | ليه |
|---|---|---|
| Root Directory | `site` | من غيرها هيدوّر على `package.json` في الجذر ويلاقي مشروع Flutter |
| **Include source files outside the Root Directory** | **مفعّلة** | `npm run build` بيبدأ بـ`theme:check`، وده بيقرا `design/palettes.json` ويقارن بـ`lib/core/theme/palettes/generated_palettes.dart` — الاتنين بره `site/`. تقفلها، البناء يقع عند `readFileSync` |
| `NEXT_PUBLIC_SITE_URL` | `https://radar-one-phi.vercel.app` | canonical والسايت ماب وصورة المشاركة |

للنشر بإيدك (لو عايز تتخطّى Git):

```bash
npx vercel deploy --prod    # من جذر الريبو، مش من site/ — لنفس سبب الجدول فوق
```

و`--prebuilt` **مبيشتغلش من ويندوز**: البناء بيطلّع symlinks في
`.vercel/output/functions/*.segments/` والرافع مش بيقدر يحلّها، فبيقع بـ
`ENOENT ... __PAGE__.segment.rsc.func`. سيب Vercel يبني على سيرفره.

قواعد Firestore لسه بتتنشر بإيدك، وده الاستخدام الوحيد الباقي لـ`firebase.json`:

```bash
firebase deploy --only firestore:rules
```

---

## حاجات معروفة وناقصة

- **الإشعارات (FCM) مش متعملة.** الإعلانات والتوصيات بقت بتتقرا وبتتعرض في
  الاتنين (تبويب «المستجدات» في الويب، و`lib/features/updates/` في التطبيق)،
  بس **مفيش إشعار بيوصل** — المستخدم لازم يفتح بنفسه.
- **صور الشارت مش بترفع.** بتتقرا من Firestore وبتتحسب في درجة الانضباط، بس
  **مش بتتعرض ولا بتتضاف** من المتصفح — دي مسارات مطلقة جوه ذاكرة التليفون.
  الواجهة بقت **بتقول ده صراحةً** في فورم الصفقة وفي تلميح درجة الانضباط، بدل
  ما تسيب المستخدم يشوف ٨٠/١٠٠ من غير سبب. الحل الكامل محتاج Firebase Storage
  (يعني خطة Blaze) ورفع من التطبيق — **اتأجّل بقرار المالك قبل الإطلاق**.
- **مزامنة الإعدادات آخر-كتابة-تكسب.** `users/{uid}/settings/risk` مالوش طابع
  زمني ولا دمج: التليفون بياخد نسخة الحساب أول ما يسجّل دخول، والويب بيقراها
  أول ما يفتح، وأي حفظ بيستبدل المستند كله. بيتلاقوا دايمًا، بس مش بالضرورة على
  آخر تعديل لو الاتنين اتعدّلوا أوفلاين. مقصود — طابع زمني من ساعة العميل بيدخّل
  عطل أسوأ (تليفون ساعته غلط بيثبّت إعداداته للأبد).
- **صورة المشاركة (OG)**: satori مش بيظبط ترتيب الكلمات العربية، فكل كلمة
  اتعملت flex item لوحدها بهوامش معايرة بالعين في
  `site/app/opengraph-image.tsx`. **لو غيّرت أي نص أو مقاس خط هناك، افحص الـ PNG
  الناتج بعينك** — مش هيظبط لوحده.

---

## أوامر

```bash
flutter analyze && flutter test          # التطبيق — لازم الاتنين يعدّوا
npm --prefix site run build              # الموقع (بيتحقق من الثيم الأول)
npm --prefix site run theme              # ولّد الألوان بعد تعديل palettes.json
node tool/gen-theme.mjs --check          # اتأكد إن المولّد متطابق
npm --prefix site run icons              # ولّد أيقونات الـ PWA بعد تغيير اللوجو
```

لفحص الـ PWA لازم بناء إنتاج — `next dev` مبيسجّلش الـ service worker عن قصد:

```bash
npm --prefix site run build && npm --prefix site run start
```

خطوات الفحص في DevTools بالتفصيل في `site/README.md`.

---

## أسلوب الشغل المتوقّع

- **اتحقق قبل ما تدّعي.** الشغل اللي فات كله اتفحص فعليًا — لقطات من مخرجات
  **الإنتاج** (مش سيرفر التطوير، اللي كان بيقدّم CSS قديم مرة)، وفحص تمدد أفقي
  عند 320 و1280، وتشغيل الحاسبة من الأول للآخر.
- **متكتبش أرقام في التسويق من دماغك.** كل رقم على الصفحة اتعدّ من الكود.
- **لو حاجة هتناقض الصفحات القانونية، قول قبل ما تعملها.**
- المالك بيتكلم عربي مصري ويفضّل الرد بيه.
