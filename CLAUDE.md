# Radar — رادار

دفتر صفقات البورصة المصرية. تطبيق أندرويد (Flutter) + موقع وداشبورد (Next.js).
الواجهة عربي RTL بالكامل. الاتنين مجانيين حاليًا.

> هذا الملف بيتحمّل تلقائيًا في أي محادثة جديدة. حدّثه لما حاجة جوهرية تتغيّر.
> آخر تحديث: 27 أغسطس 2026.

---

## الحالة الحالية

| | |
|---|---|
| الفرع | `main` **مقفولة على الدفع المباشر** — الشغل بيروح على فرع وبعدين PR. حارسين: branch protection على GitHub (`enforce_admins` مفعّلة) و`tool/hooks/pre-push` محليًا. شوف «النشر» |
| الاختبارات | `flutter analyze` نضيف · `tsc` نضيف · `flutter test` **٥٣١ كلهم بينجحوا** (٩ متخطّيين — الباقات متوقّفة) |
| الحالة والتخزين | **Bloc/Cubit + Firestore وبس** — صفر Riverpod، صفر Hive، والحزمتين اتشالوا من `pubspec.yaml` (٢٨ أغسطس). شوف §٢٥ |
| النشر | **الموقع نازل** على <https://radar-one-phi.vercel.app> (مشروع Vercel `radar`) · **قواعد Firestore اتنشرت** (٢٧ أغسطس) · **التطبيق مش هيترفع على Play قريبًا** — قرار المالك |
| مشروع Firebase | `trademind-6222c` (اسم العرض Radar) — مربوط في `.firebaserc` فمش محتاج `--project`. بقى للـ Auth وFirestore بس |

الاسم اتغيّر من TradePilot لـ **Radar** في كوميت `f776f16`. اسم الحزمة الداخلي في
`pubspec.yaml` لسه `egx_trade_journal` — ده اسم داخلي مالوش أثر على المستخدم،
وتغييره بيلمس كل ملف import في المشروع، فاتساب عن قصد.

---

## البنية

```
lib/                    تطبيق Flutter
  billing/              الاشتراك والتجربة — entitlements.dart نقي ومتختبر
  core/calc/            منطق حسابي بحت — صفر Flutter imports (ده قيد صارم)
  core/theme/           الثيم، والباليتة مولّدة (متعدّلهاش بإيدك)
  core/state/           AccountScopedCubit (الأساس المشترك) + app_state.dart
                        (قراءات `context.trades/.settings/…`) + state_views.dart
                        (TradesBuilder · WatchlistBuilder · SettingsGate)
  core/preferences/     تفضيلات الجهاز في SharedPreferences + كيوبت لها
  */data/ */cubit/      كل ميزة: repository على Firestore + Cubit فوقه
  features/auth/        Firebase Auth — تسجيل الدخول إجباري (AuthCubit)
  features/sync/        اللي فضل: حذف الحساب + مستند الاشتراك
  features/market/      أسعار الإغلاق (Yahoo endpoint غير رسمي)
  features/ai_parser/   قراءة توصية من صورة بـ Gemini (بمفتاح المستخدم)
  trades/ watchlist/ today/ dashboard/ calculator/ settings/ shell/
    billing/widgets/plan_card.dart    الباقة، وحالة قراءة الاشتراك
    settings/widgets/legal_tiles.dart التنويه وروابط المستندات القانونية

site/                   Next.js 16 + Tailwind 4 — على Vercel، مش تصدير ثابت
  app/(marketing)/      الصفحة الرئيسية + الصفحات القانونية (بهيدر وفوتر)
  app/(app)/            /dashboard و /admin (شل بسيط، من غير الهيدر التسويقي)
  lib/                  firebase, auth-context, trade, risk-math, format, site
                        + account-settings.ts (قاعدة المخاطرة من الحساب)
                        + projection.ts (توصل إمتى بالتداول — مرآة
                          goal_projection.dart)
                        + goal-plan.ts (تحطّ كام بالادخار — مرآة goal_plan.dart)
                        + portfolio-scenarios.ts (أحسن/أوحش حالة — مرآة
                          portfolio_scenarios.dart)
                        + sizing.ts (حجم المركز — مرآة sizing_result.dart)
                        + smart-trade.ts (خطة الصفقة كاملة — مرآة
                          smart_trade.dart)
                        + egx-directory.ts (٣٠ سهم بأسمائهم — مرآة egxDirectory)
                        + ai-parser.ts (قراءة توصية من صورة — مرآة
                          ai_trade_parser_service.dart) + gemini-key.ts
                        + quote.ts + use-quotes.ts (آخر إغلاق للمراكز المفتوحة)
                        + subscription.ts (مرآة entitlements.dart) +
                          use-subscription.ts
                        + market-flows.ts (تحليل صفحة البورصة — pure، متختبر)
                        + egx-fetch.ts (الشبكة بس) + market-flows-store.ts
  app/api/egx-flows/    أول كود سيرفر في المشروع — بيجيب ويحلّل، مش بيكتب
  app/api/quote/        آخر إغلاق من Yahoo (الرمز بيتفحص مقابل الدليل الأول)
  components/dashboard/ الداشبورد الاتنين + بانل تسجيل الدخول + محرّر التايم لاين
                        + goal-panel.tsx (تبويب «الهدف»)
                        + scenarios-panel.tsx (سيناريوهات المحفظة، جوّه «الأداء»)
                        + market-flows-panel.tsx (قسم «السوق»)
                        + quick-add-sheet.tsx (إضافة صفقة سريعة — مرآة
                          quick_add_trade_sheet.dart)
                        + ticker-field.tsx (ترشيح رمز السهم وهو بيكتب)
                        + ai-trade-sheet.tsx (شيت التحليل بالـ AI)
                        + paywall.tsx (الجدار + شريط التجربة + كارت الباقة)
                        + legal-notice.tsx (التنويه والروابط — جوّه «الإعدادات»
                          مش تحت كل تبويب)
                        + quote-badge.tsx (آخر إغلاق تحت خانة الرمز — مرآة
                          stock_quote_badge.dart)
  app/manifest.ts       PWA manifest (راوت، مش ملف ثابت)
  public/sw.js          service worker — أوفلاين + شرط التثبيت
  components/pwa.tsx    التسجيل وزرار «ثبّت التطبيق»

design/palettes.json    مصدر الألوان الوحيد للتطبيق والموقع
tool/gen-theme.mjs      بيولّد باليتة Dart + توكنز CSS من الملف اللي فوق
firestore.rules         قواعد الأمان — فيها نموذج الأدمن
```

---

## قواعد لازم تتحط في الاعتبار قبل أي تعديل

### ١. الألوان ليها مصدر واحد — متعدّلش الملفات المولّدة

كل لون بيتولّد من `design/palettes.json`:

```bash
npm --prefix site run theme
```

بيكتب `lib/core/theme/palettes/generated_palettes.dart` و `site/app/tokens.css`،
والاتنين عليهم `DO NOT EDIT`. و`npm run build` بيرفض يكمّل لو اتفرّقوا عن المصدر.

**تلات قواعد مفروضة في المولّد نفسه:**

- **ألوان الفلوس محميّة.** `win` و`loss` و`breakeven` في بلوك `semantics` بره
  الباليتات، وأي باليتة تحاول تعيد تعريفهم المولّد بيرفض يشتغل. السبب: المتداول
  بيتعلّم إن الأخضر = ربح؛ لو تغيير الثيم قادر يعيد صبغه، اللون يفقد معناه.
- **بوابة تباين.** 30 زوج بيتقاسوا في كل باليتة قبل أي كتابة. الرسوب بيوقف
  التوليد.
- **`brand` خلفية مش نص.** الباليتة النشطة (`pharaoh`) لونها ليمون `#D9FF30`،
  تباينه **1.15:1** على الأبيض. النص الملوّن بالبراند بياخد `--brand-ink`
  (وهو `accent`)، والزراير بس هي اللي بتستخدم `bg-brand`.
  في Flutter: `ColorScheme.primary` مربوط بـ `accent` **مش** `brand` — لأن
  Material بيستخدم `primary` كخلفية وكنص في نفس الوقت.

**ونفس الفخ في التطبيق**: `SnackBarThemeData` خلفيتها `inverseSurface`، وكان
النص فيها `Colors.white` ثابت — يعني في الوضع الداكن أبيض على كريمي، وكل رسالة
في التطبيق مش بتتقرا. النص والزر الاتنين بياخدوا `onInverseSurface` دلوقتي،
و`snack_bar_theme_test.dart` بيقيس التباين في البرايتنسين.

**فخ متكرر**: `inverse-surface` **بينقلب مع الثيم** (فحمي في الفاتح، كريمي في
الداكن). ممنوع تحط `text-brand` جوّاه — بيطلع 1.01:1 في الوضع الداكن. استخدم
`text-on-inverse-surface`، والليمون يدخل كـ**بادج مملوء** بنص غامق.

### ٢. الأرقام: إبسيلونات، وأرقام غربية

`lib/core/calc/risk_math.dart` فيه `kQtyEpsilon` و`kRiskEpsilon`، والاتنين
**مرتبطين ببعض**. `exceedsRiskLimit()` هي **المكان الوحيد** اللي ينفع تتقارن فيه
نسبة مخاطرة — متكتبش `riskPct > maxRiskPercent` في أي مكان تاني.

كل الـ formatters مبنية على locale `'en'` **عن قصد** — locale `'ar'` بيطلّع أرقام
عربية هندية. و`parseNumber` بيقبل `٠-٩` لأن الكيبورد العربي بيطلّعها.
`site/lib/risk-math.ts` و `site/lib/format.ts` نسخ مطابقة للنسخة الـ Dart.

### ٣. الصفحات القانونية جزء من الكود

`site/app/(marketing)/privacy/page.tsx` و `terms/page.tsx` **مستندات قانونية
منشورة**، ومراجع Play بيقراها. أي تغيير في سلوك التطبيق بيلمس بيانات المستخدم
لازم يتحدّث فيها **في نفس الكوميت**.

سبق وحصل: لما تسجيل الدخول بقى إجباري، كان فيه **٥ أماكن** بتقول «شغّال من غير
حساب» — واحدة منهم في سياسة الخصوصية وواحدة في الشروط.

كمان: `RELEASE.md` بيقول إن الوصف لازم يقول صراحةً إن المنتج **مش نصائح
استثمارية** عشان Play مايصنّفوش تحت الفئات المالية المقيّدة. الجملة دي في
`site/lib/site.ts` باسم `disclaimer` وبتظهر في الفوتر والشروط والخصوصية.

**وفي الداشبورد هي في «الإعدادات»** (`legal-notice.tsx`)، مش في
`app/(app)/layout.tsx`. كانت في الليّاوت فبتتثبّت تحت كل تبويب — يعني شاشة
«قرار اليوم» الفاضية على التليفون بتنتهي بفقرة قانونية. الجملة لسه ظاهرة في كل
صفحة تسويقية عن طريق `SiteFooter`، وهي دي الصفحة اللي مراجع Play بيقراها.

**و«التطبيق» في المستندات القانونية معناها تطبيق أندرويد وبس.** كانت مستخدمة
كاسم المنتج، فالشروط كانت بتحكم «التطبيق» بينما كل المستخدمين على **موقع** —
اتصلّحت في ٥ أغسطس، وكل جملة بتتكلم عن المنتج بقت بتقول «رادار». لو ضفت جملة
جديدة، خد بالك من الفرق ده.

### ٤. الأمان — نموذج الأدمن

الأدمن = وجود مستند في مجموعة `admins`، و**مفيش أي مسار عميل يقدر يكتب فيها**
(`allow write: if false`). بتتعمل من Firebase Console بس.

الفلاج `isAdmin` في React **بيقرر الشكل بس**. كل استعلام وراه بيتفحص تاني على
السيرفر.

**الأدمن مش بيقرا صفقات أي حد** — بيشوف ملفات التعريف والعدّادات بس. ده مقصود:
سياسة الخصوصية بتقول إن قواعد السيرفر بتمنع أي مستخدم تاني من قراءتها. لو حد
طلب تغيير ده، لازم السياسة تتغيّر وتتعاد إتاحتها **الأول**.

**وعشان كده `capital` عايش في مجموعة فرعية.** إعدادات المخاطرة في
`users/{uid}/settings/risk` مش كحقول على ملف التعريف — لأن ملف التعريف هو
المستند الوحيد اللي الأدمن مسموح له يقراه، ورأس المال ده حجم محفظة المستخدم.
منح القراءة على مستند مبيوصلش لمجموعاته الفرعية، فالقاعدة هنا هي نفس قاعدة
الصفقات: المالك وبس.

### ٥. الويب بيكتب في Firestore — والتطبيق بيقرا اللي بيكتبه

`site/lib/trade.ts` و`watchlist.ts` فيهم `encodeTrade`/`encodeWatchlistItem`،
ولازم يفضلوا مطابقين لـ `SyncCodec` بالحرف. **أربع قواعد لو اتكسرت البيانات
بتتلف بصمت:**

1. **التواريخ نصوص ISO مش Timestamps.** الـ codec في Dart بيعمل
   `value is String ? DateTime.tryParse(value) : null` — تديله Timestamp
   يبقى التاريخ `null`، ولـ`entryDate` ده معناه إن كل صفقة من الويب تاريخها
   يقفز لـ«دلوقتي» على التليفون.
2. **مفتاح المستند = `trade.id`**، والكتابة `merge: true`. غير كده نفس الصفقة
   بتتفرّع لمستندين.
3. **`screenshotPaths` بيتقرا وما بيتكتبش أبدًا.** كتابته فاضية مع الـ merge
   بتمسح صور الشارت اللي التليفون كتبها — من واجهة أصلاً مش بتعرضها.
4. **`timeline` بيتكتب، بس بالمصفوفة كاملة.** Firestore بيستبدل حقل المصفوفة
   مش بيدمج عناصره، فأي حفظ لازم يبدأ من القايمة المخزّنة — الفورم بيتعبّى من
   الصفقة المحمّلة عشان كده بالظبط. (كان محذوف زي `screenshotPaths` لحد ما
   بقى فيه محرّر تايم لاين في `site/components/dashboard/timeline-editor.tsx`؛
   حذفه كان بيخلي «آخر لمسة» = تاريخ الدخول دايمًا، و«محتاجة ملاحظة» تشتغل على
   صفقات التطبيق شايفها متابَعة.)

الحسابات كلها (`analytics.ts`، `risk-score.ts`، `decisions.ts`، `checklist.ts`،
`projection.ts`، `goal-plan.ts`، `portfolio-scenarios.ts`، `smart-trade.ts`) نسخ
مطابقة من `lib/core/calc/`.

> **`sizing.ts` مبقاش واحد منهم — بقى بينده الـDart نفسه.** شوف §٢٤ تحت.

**ومفيش أي اختبار على نسخ الويب** — مفيش `test` script في `site/package.json`
ولا ملف اختبار واحد في `site/`. الاستثناء الوحيد `test/legal_notice_test.dart`:
ده اختبار Dart **بيقرا `site/lib/site.ts` نفسه** ويقارن نص التنويه وبريد
التواصل حرف بحرف، فالجملة القانونية مش قادرة تفترق بين السطحين في صمت. نفس
الحيلة تنفع لأي ثابت مشترك تاني. الحارس الوحيد هو اختبارات Dart المقابلة
(`risk_score_test.dart`، `daily_decisions_test.dart`،
`journal_analytics_test.dart`، `goal_projection_test.dart`،
`portfolio_scenarios_test.dart`). يعني لو غيّرت معادلة في Dart، الاختبارات
هتمسكها هناك بس، ونسخة الويب هتفضل غلط بصمت لحد ما حد يبص عليها بعينه.
**غيّرها في الاتنين في نفس الكوميت.**

**والاستثناء اللي كان مكتوب هنا اتقفل (٢٨ أغسطس).** نسبتَي الهدف والاستوب
الافتراضيتين كانوا **متحطوطين في الكود** على الويب (`5%` و`2%`) بينما التطبيق
بيقراهم من `Settings` — فنفس الصفقة كانت بتاخد حكمين مختلفين لأي حد غيّر
افتراضي. الحقلين دخلوا `users/{uid}/settings/risk`، والقواعد اتوسّعت من ٣ حقول
لـ٥، و`account-settings.ts` بيقراهم ويمرّرهم لـ`portfolioScenarios`.

**والمستند بيتكتب استبدال كامل من الويب (`setDoc` من غير merge).** يعني أي
`update` لازم يحمل **الخمسة**، وإلا بيمسح اللي التليفون كتبه. القاعدة دي هي
اللي بتخلي إضافة حقل سادس في المستقبل تلمس `LocalSettings` و`DEFAULTS`
و`sanitise` و`writeCache` مرة واحدة.

### ٦. تلات أنواع مختلفة — متخلطهمش تاني

الدفتر فيه **تلات حاجات مش نفس الحاجة**، والفرق ده مبني في البيانات من الأول:

| النوع | إزاي | فين |
|---|---|---|
| **صفقة حقيقية** | `status` = `open` أو `closed` | تبويب «صفقاتي» |
| **فكرة / تخطيط** | `status` = `planned` أو `cancelled` | تبويب «تخطيط» |
| **سهم بيراقبه** | مجموعة `watchlist` منفصلة | تبويب «قائمة المراقبة» |

**والفصل ده في الاتنين**: `TradesFilter` في `lib/trades/trades_screen.dart`
و`realTrades`/`plannedTrades` في `customer-dashboard.tsx`.

**وأسماء التبويبات لازم تتطابق حرفيًا بين الموقع والتطبيق** — قرار اليوم ·
صفقاتي · تخطيط · الأداء · التحليلات · قائمة المراقبة · تصدير CSV، بنفس الترتيب
على السطحين.

> **«قائمة المراقبة» كانت في قايمة الثلاث نقط في التطبيق، والقايمة دي اتشالت.**
> ومعاها راح **الطريق الوحيد** لوضع الإضافة في `WatchlistFormScreen` — الفورم
> بيسمّي نفسه «إضافة للمتابعة» لما ييجي من غير `existing`، ومحدش كان بينده عليه
> كده. الزرار الوحيد كان «تعديل» على كارت في «قرار اليوم»، وده بيتطلّب عنصر
> موجود أصلًا. يعني المستخدم يقدر يضيف من الموقع ويعدّل من التليفون، بس **مش
> قادر يضيف من التليفون** — وعلى حساب جديد الميزة مختفية تمامًا من التطبيق.
> بقت تبويب في نفس مكانها على الموقع. كانت مختلفة (اليوم/قرار اليوم،
الأداء/نظرة عامة) لنفس الشاشة بالظبط، وده بيخلي المستخدم يفتكر إنهم منتجين.
**والترتيب بقى متطابق كمان** عشان الذاكرة العضلية تعدّي بين الاتنين.
`acceptance_test.dart` و`today_screen_test.dart` بيتحققوا من القايمة دي، فأي
تغيير في اسم لازم يعدّي عليهم.

كانوا كلهم في جدول واحد، والشارة كانت بتعدّهم مع بعض — ٩ أفكار و٣ صفقات
بيطلعوا «١٢»، رقم مالوش معنى.

**قاعدتين لازم يفضلوا:**

- **الأعمدة بتختلف حسب النوع، مش فلتر على نفس الجدول.** الصفقة اللي ماحصلتش
  مالهاش ربح ولا `R` ولا درجة انضباط — دول بيحكموا على قرار بنتيجته، والفكرة
  مالهاش نتيجة. «—» في عمود الربح بتتقرا «تعادل» بسرعة العين، ودرجة انضباط على
  خطة ما اتنفذتش بتدعو القارئ يقيّم حاجة ماحصلتش. `TradesTable` بياخد
  `variant`: `'real'` بيعرض الفلوس، و`'planned'` بيعرض المخاطرة ونسبتها.
- **الخروج هو اللي بيقفل الصفقة، مش المنتقي.** صفقة `open` اتكتب لها سعر
  وتاريخ خروج بتتحفظ `closed` (`resolvedStatus` في `trade_form_saver.dart`
  و`trade-form.tsx`)، والعكس: تغيير النوع لأي حاجة غير «مغلقة» بيفضّي
  الخانتين (`_changeStatus` / `changeStatus`) — من غير ده إعادة فتح صفقة
  مستحيلة، لأن السعر بيفضل في الخانة والحفظ بيقفلها تاني على طول.
  و`closed` من غير خروج مرفوضة بصوت عالي (سناك بار **قبل** `validate()`، لأن
  الفورم ListView كسول والخانة تحت الطية فالخطأ بيتكتب في مكان مش مبيّن).
  السبب: `Trade.isOpen` هي `exitPrice == null`، فالسجل ده بيناقض نفسه.
  كان زر «إقفال» بيوعد بالقفل ويوديك لفورم بيحفظ النوع حرفيًا — فالصفقة تفضل
  «مفتوحة» في قرار اليوم وبرّه معدل النجاح ومنحنى رأس المال.

- **الأداء بيتقاس على `realTrades` بس.** `hasTrades` مربوطة بيهم مش بطول
  `trades` — اللي عنده خطط بس المفروض يشوف الحالة الفاضية اللي بتقوله يعمل إيه،
  مش صفحة تحليلات كل أرقامها «—». والتحليلات وحاسبة الهدف أصلاً بيحسبوا
  المغلقة بس.

والحاسبة السريعة في تبويب «تخطيط» **مبتحفظش أي حاجة** — ده كان الناقص: اللي
عايز يحسب حجم مركز وبس كان لازم يعمل صفقة «مخططة» ويحفظها، فيلوّث الدفتر
بحسابات مرمية.

### ٧. شل الموبايل مرآة لـ `HomeShell` — والـ `.num` فخ اتجاهي

**التصميم على الموبايل مبني على شل التطبيق حرفيًا**، مش تكيّف للموقع:

| | التطبيق | الموقع |
|---|---|---|
| ٤ وجهات في بار تحت | `HomeShell` → `NavigationBar` | `SECTIONS` في `customer-dashboard.tsx` |
| ٦ تبويبات جوّه «صفقاتي» | `TradesHubScreen` → `TabBar(isScrollable)` | شريط أفقي بـ`overflow-x-auto` وتحته خط |
| زرار عايم «أضف صفقة» | `FloatingActionButton.extended` | زرار `fixed` — **في قسم «صفقاتي» بس** |

الوجهات: صفقاتي · السوق · حاسبة الصفقة · الإعدادات. من `sm` وطالع البار بيتحوّل
لصف فوق ومفيش بار تحت — **نموذج تنقّل واحد، مرسوم في المكان اللي كل شاشة
مستنياه فيه**. كان في الموقع صف واحد فيه ٨ أزرار بتلفّ على تلات صفوف على
التليفون.

و**حاسبة الصفقة بقت وجهة مستقلة** بدل ما تكون فوق قايمة الصفقات المخططة، زي
التطبيق بالظبط. و**الإعدادات بقت وجهة** فيها رأس المال والحساب وزراير الخروج
والثيم — كانت شريط مطويّ فوق كل تبويب.

**والشبكات ٢ في الصف على التليفون** (`grid-cols-2`)، مش عمود واحد — ده
`crossAxisCount: 2` بتاع `_StatGrid`.

#### الفخ: `.num` بيقلب الاتجاه

`.num` في `globals.css` فيه `direction: ltr`. يترتب عليه حاجتين **بيكسروا الشكل
من غير أي رسالة خطأ**:

1. **ممنوع تحطه على فقرة نص عربي.** السطر كله هيتقرا LTR، و«%» أو «ج.م» في
   آخره هيقفز لأول السطر — طلع فعلًا «‎%دخول 78.40 ج.م · مخاطرة 6.9». حطّه على
   **الرقم لوحده** جوّه `<span>`.
2. **الهوامش المنطقية عليه بتروح للناحية الغلط.** `ms-2` على عنصر `.num` بتتحسب
   على LTR فبتبقى يسار بدل يمين، والعدّاد بيلزق في العنوان («فوق حد المخاطرة3»).
   استخدم `gap` على الأب الـflex — ده مالوش علاقة باتجاه الابن.

### ٨. إضافة صفقة: شيت سريع الأول، والفورم الكامل ورا لينك

`quick-add-sheet.tsx` مرآة لـ`quick_add_trade_sheet.dart`: ست خانات (رمز،
دخول، استوب، **المبلغ اللي هدخل بيه**، هدف، عدد) + حساب حجم حي + تحذير تجاوز
المخاطرة. خانة المبلغ بتقيّد المقترح بالكاش الفعلي — من غيرها القاعدة بتحسب
كأن الحساب كله ورا كل صفقة، فبتقترح مئات الأسهم على واحد جاي يدخل بألفين جنيه.
(`SizingResult.compute` و`computeSizing` عندهم `budget` من الأول؛ الحاسبة كانت
بتستخدمه والشيت لأ.) والفورم الكامل مالوش خانة مبلغ، فـ«التفاصيل الكاملة ←»
بتنقل **الكمية** اللي المبلغ طلّعها بدل ما ترميها. الموقع كان بيفتح
الفورم الكامل — منتقي النوع والتواريخ والسبب والتشيك ليست والتايم لاين — لنفس
الفعل.

تلات قواعد:

- **الشيت بيحفظ `planned` دايمًا**، و`reason` = «صفقة سريعة»، زي التطبيق. تسجيل
  إن الصفقة اتنفّذت هو القرار اللي منتقي النوع في الفورم الكامل موجود عشانه،
  وخمس خانات مش بتسأله.
- **«التفاصيل الكاملة ←» بتنقل اللي اتكتب**، متمسحوش — بتروح `view.seed`.
  وعشان كده الفورم بيعتبر `0` في السعر أو الكمية = خانة فاضية، مش قيمة.
- **الاسم واحد في كل مكان**: `ADD_TRADE_LABEL` = «أضف صفقة». كان في الموقع
  «صفقة جديدة» و«سجّلها كصفقة» و«سجّل أول صفقة» لنفس الحاجة — نفس الغلطة اللي
  التطبيق صلّحها بـ`kAddTradeLabel`.

**وترشيح رمز السهم** (`ticker-field.tsx` + `egx-directory.ts`) بيشتغل من أول
حرف، بالكود أو بالاسم العربي، من نفس الـ٣٠ سهم اللي في `egxDirectory`. **ده
تعريف باسم السهم، مش ترشيح استثماري** — القايمة ثابتة وواحدة لكل المستخدمين،
ومفيش فيها ترتيب ولا تقييم؛ الترشيحات ممنوعة (شوف جدول القرارات).

> **فخ `autoFocus`:** بيتحوّل لخاصية `autofocus` في HTML بتاع السيرفر، فالمتصفح
> بيعمل focus وهو بيقرا الصفحة — **قبل** ما React يركّب `onFocus`. الحدث ده
> مبيتعادش، فالحقل بيفضل focused والـstate فاكره mounted بس. `ticker-field.tsx`
> بيقرا `document.activeElement` مرة واحدة في `useEffect` عشان كده — من غيرها
> الاقتراحات ما كانتش بتظهر خالص.

### ٩. مفيش مزامنة — فيه مخزن واحد

**البند ده كان بيشرح دمجًا ثلاثيًا ببصمات، وهو اتشال بالكامل في ٢٨ أغسطس.**
مكتوب هنا لأن اللي اتشال بيوضّح ليه اللي مكانه بسيط كده.

كان فيه مخزنان — بوكس Hive على التليفون ونسخة في Firestore — فكان لازم كود
يقرّر مين يكسب لكل `id`، مقابل **سلف مسجّل** في بوكس `syncState`. ده كان
`sync_merge.dart` (١٨٧ سطر و٢٠ اختبار) و`sync_provider.dart` (٣٣٩ سطر)، ومعاهم
قاعدة «ممنوع تستخدم `updatedAt` في القرار» وحفرة معروفة: **الحذف مش بيتنقل**،
لأن «اتمسح» و«الجهاز ده عمره ما شافه» مالهمش فرق من غير tombstones.

بمخزن واحد مفيش تفرّع أصلاً يتدمج. الـrepositories في `lib/*/data/` بتقرا
`snapshots()` وبتكتب على طول، والحذف بيفضل محذوف. راح معاه:
`sync_merge.dart` · `sync_provider.dart` · بوكس `syncState` · التلات adapters ·
`hive_keys.dart` · وتمان دوال push/pull من `FirestoreSyncService` (اللي فضل فيه
حذف الحساب ومستند الاشتراك وبس).

**اللي دخل مكانه**: `AccountScopedCubit` — تابع حساب، سيب اللي قبله، اصمد قدام
تجديد التوكن (Firebase بيعيد إصدار نفس الـuid كل ساعة، وإعادة الاشتراك عليه
بتبيّض الشاشة وتشتري قراءة جديدة)، ونضّف عند الإغلاق. `retry()` موجودة لأن حالة
فشل من غير طريق رجوع بتسيب الشاشة على رسالة خطأ لحد ما التطبيق يتقفل.

**وثمن المخزن الواحد**: كل قايمة بقى ليها **حالة تحميل حقيقية**. الفرق بين
«مفيش صفقات» و«مش قادرين نقرا صفقاتك» هو الفرق بين حالة فاضية صح وأسوأ جملة
ممكن دفتر يقولها لواحد عنده صفقات — عشان كده الحالات أربعة
(`Loading/Loaded/Failure/SignedOut`) وفيه `TradesBuilder` و`WatchlistBuilder`
بيفكّوها في مكان واحد.

**و`SettingsGate` بوابة مش بيلدر**، والفرق مقصود: رأس المال وسقف المخاطرة
بيتقسموا في كل حجم مركز وكل نسبة مخاطرة في المنتج، على شاشات مش بتذكر
الإعدادات أصلًا. مفيش مكان تحط فيه سبينر، وترك الافتراضيات تشتغل لفريم واحد
مبيوريش «لا حاجة» — بيوري **رقم واثق وغلط**. فبتستنى مرة واحدة فوق كل حاجة،
وتحتها `context.settings` مش null بالبناء.

**وكاش Firestore المحلي شغّال** (افتراضي على الموبايل) وهو **مش مخزن تاني**:
مفيهوش إجابة السيرفر ما ادّهاش، ومصدر الحقيقة يفضل السيرفر. قفله معناه شاشة
فاضية مع أول لحظة نت ضعيف بدل آخر نسخة معروفة.

**والموقع بيسمع حيًّا** بـ`onSnapshot` مش `getDocs` مرة واحدة، فأي حاجة التليفون
يرفعها بتظهر من غير refresh.

### ١٠. الـ AI والأسعار: تلات قرارات في المسار

**مفتاح Gemini بتاع المستخدم مش بيتحط في Firestore.** عايش في `localStorage`
(`site/lib/gemini-key.ts`)، زي ما التطبيق حاططه في `SharedPreferences`. ده
اعتماد طرف تالت
بيتحاسب عليه المستخدم؛ حاجة مبتخرجش من الجهاز اللي اتكتبت عليه مبتتكشفش بغلطة
في قواعدنا ولا في النسخ الاحتياطي ولا في لوحة الإدارة. التكلفة إنه **مبيتزامنش**
— والإعدادات بتقول كده صراحةً بدل ما المستخدم يكتشفها.

**ونداء Gemini بيروح من المتصفح لـGoogle على طول**، مش من عندنا. لو عدّيناه
علينا كنا هنمرّر اعتماد كل مستخدم على سيرفرنا — سر نحرسه واختراق نملكه — مقابل
لا حاجة، لأن المفتاح أصلًا بتاع المستخدم وفي متصفحه، زي ما هو على تليفونه.

**بالعكس، `/api/quote` لازم يكون سيرفر**: Yahoo مبيبعتش CORS headers فالطلب
بيتمنع قبل ما يخرج من المتصفح. **والرمز بيتفحص مقابل `EGX_DIRECTORY` قبل
الاستخدام** — الراوت بيحط مدخله في URL خارج، فبارامتر مفتوح كان هيخليه بروكسي
يجيب أي حاجة على egress بتاعنا. تلاتين كود ثابت، وأي حاجة غيرهم بتترفض.

وده فرق في تدفّق البيانات لازم يفضل مكتوب في سياسة الخصوصية: **على الموقع
سيرفرنا بيشوف IP المستخدم وقت جلب السعر**، وعلى التطبيق الطلب بيروح من التليفون
للمصدر على طول. اتضاف في بند ٤.

**والتطبيق بيجيب السعر من `/api/quote` هو كمان، مش من Yahoo مباشرة.** كان كل
واحد بينده مصدره، فنفس المركز المفتوح كان بيوريك سعرين مختلفين على التليفون
وعلى الموقع. المتصفح **مبيقدرش** ينده Yahoo (مفيش CORS)، فالراوت بتاعنا هو
الـendpoint الوحيد اللي الاتنين يقدروا يتشاركوه. وفيه **fallback** في التطبيق
بينده Yahoo مباشرة لو الراوت مش شغّال — ساعتها الموقع مش بيوري سعر أصلًا،
فمفيش اختلاف يظهر.

**وكل مسار فشل في السعر بيرجّع «مفيش سعر»، عمره ما يرجّع صفر.** سعر ما وصلش لازم
ما يبانش كأنه مركز ما اتحركش — و`0` كان هيتحسب خسارة ١٠٠٪.

**والتحليل بالـ AI بيحفظ `planned` دايمًا** بـ`reason` = «توصية من صورة». دي
توصية وصلت للمستخدم من حد تاني، مش صفقة عملها — والفرق ده هو نفسه اللي بيخلي
الميزة **مش** ترشيح استثماري مننا: بنقرا صورة المستخدم معاه أصلًا.

> `parseResponse` فيها فروع اتكتبت من ردود Gemini حقيقية، وكل واحدة ليها رسالة
> مختلفة عن قصد — رد مقطوع، شكل غير متوقع، رفض، `MAX_TOKENS`. النسخة القديمة
> كان فيها `try/catch` واحد حوالين الجسم كله وكانت بتحوّلهم كلهم لـ«رد التحليل
> مش مفهوم». و**Gemini 3 بيقسّم الرد على أكتر من `part` وبيبعت أجزاء تفكير
> جنبه** (`thought: true`)، فأخد أول جزء بس كان بيقطع الـJSON في نصه.

### ١١. الباقات: السيرفر هو الحكم، والعميل بيرسم بس

**١٤ يوم تجربة** لكل حساب جديد، وبعدها الدفتر والحاسبات مجانية للأبد، والأربع
مميزات المدفوعة بتتقفل: السوق · الأسعار · التحليل بالـAI · الأداء والتحليلات.

المستند: `users/{uid}/billing/subscription` — `plan` و`trialStartedAt`
و`proUntil` و`note`.

**التجربة مبتتزوّرش، وده سبب وجودها في القواعد أصلًا:**

- المستخدم يقدر **يعمل** المستند مرة واحدة بس وبشكل `trial` بس (Firestore
  بيقيّم `create` لما المستند مش موجود، فـ«مرة واحدة» مجانية).
- **`trialStartedAt` لازم يساوي `request.time`** — ساعة السيرفر. عميل بيبعت
  تاريخ من عنده بيترفض.
- **عشان كده تاريخ النهاية مش متخزّن.** مفيش sentinel لـ«وقت السيرفر + ١٤ يوم»،
  فتاريخ نهاية مخزّن كان لازم ييجي من ساعة الجهاز ويتعدّل بسهولة. النهاية
  بتتحسب من البداية + الثابت المشترك.
- **المستخدم مبيقدرش يعمل `update` خالص.** الترقية لـ`pro` كتابة أدمن بس.

**الأدمن بيقرا المستند ده** — عشان يفعّل دفعة أو يرد على «ليه اتقفل عليّا». ده
**استثناء وحيد** على قاعدة «الأدمن مبيشوفش أي حاجة تحت ملف التعريف»، وسياسة
الخصوصية اتحدّثت قبل ما ينزل. مفيهوش أي حاجة عن الصفقات ولا رأس المال.

#### اللي القواعد تقدر تفرضه واللي متقدرش

**واحدة بس من الأربعة بيانات إحنا اللي عندنا** — `marketFlows` — وعشان كده هي
الوحيدة اللي ليها قاعدة (`hasActivePlan()`). الباقي **جدران عميل وبس**:

- **الأداء والتحليلات** بتتحسب على الجهاز من صفقات المستخدم نفسه. مفيش طلب
  يتصرّح، والأرقام عمرها ما بتخرج. قاعدة مش هتوصلها أبدًا.
- **التحليل بالـAI** شغّال بمفتاح المستخدم وبيتحاسب عليه هو. قفله مش بيحمي
  تكلفة عندنا.
- **`/api/quote` مفيهوش أي اعتماد لحد دلوقتي.** قفله محتاج الراوت يتحقق من
  Firebase ID token، وده محتاج `firebase-admin` وservice account — **دين مكتوب
  مش افتراض متعمّل**.

**والثابت ١٤ مكتوب في تلات أماكن** (`kTrialDays` · `TRIAL_DAYS` · القاعدة)،
لأن ملف قواعد مبيقدرش يستورد حاجة. أي تغيير لازم يعدّي على التلاتة.

**والدفع يدوي بقرار المالك، مش نقص مؤقت اتسرّب.** المستخدم بيبعت طلب من
`subscribe-dialog.tsx` (رسالة بريد جاهزة فيها بريده ومعرّف حسابه والباقة)،
بيدفع بأي طريقة اتفقتوا عليها، والأدمن بيفعّل من `ActivationDialog` اللي **بيسجّل
المبلغ والطريقة ومرجع التحويل** في `note`. عملية يدوية من غير سجل للمبلغ هي
عملية مش قادرة ترد على «أنا دفعت» بعد تلات شهور.

والواجهة **بتقول إن مفيش دفع أونلاين** صراحةً، لأن زرار شراء بيودّي لحتة مش
موجودة أسوأ من مفيش زرار. وعلى أندرويد أي اشتراك رقمي لازم يعدّي على **Play
Billing** — مش بوابة دفع عادية.

**والمزامنة السحابية مجانية، مش ميزة مدفوعة.** كانت في قايمة Pro واتشالت في
٦ أغسطس: تسجيل الدخول إجباري والنسخة السحابية هي **إزاي** الدفتر بيشتغل أصلًا،
والتطبيق والموقع نفس الحساب. مستخدم مجاني صفقاته وقفت تتزامن مش عنده نسخة
محدودة، عنده منتج مكسور.

**وممنوع تكتب «أسعار لحظية».** `/api/quote` بيرجّع **آخر إغلاق يومي** من
endpoint غير رسمي. الادعاء ده كان في ٥ أماكن واتصلّح — يوم ما يبقى فيه مزوّد
بيانات لحظية مرخّص، يترجع.

**والباقة المجانية بتفضل تكتب في الدفتر.** `Feature` فيه أربع قيم وتسجيل
الصفقات مش واحد منهم، وفيه اختبار بيثبّت ده: دفتر بيوقف يخليك تكتب فيه مش باقة
محدودة.

### ١٢. حذف الحساب — عليه اختبار حارس

`test/account_deletion_test.dart` بيقرا سورس `auth_repository.dart` ويتأكد إن
`deleteAccount` بيمسح **`users/{uid}` كمان** مش الصفقات بس، و**قبل** ما يمسح
الهوية. السبب: سياسة الخصوصية (بند ٦) وصفحة `/delete` بيوعدوا بمسح «حساب الدخول
(البريد والاسم المعروض)»، وPlay بيطلب نفس الحاجة. والمستند ده بعد مسح الهوية
**بيبقى مستحيل الوصول له للأبد** — القواعد بتديه لصاحبه بس، وصاحبه اتمسح.

الاختبار بيقرا الملف مش بينده الدالة، لأن كل الدوال دي بتقف عند
`Firebase.apps.isNotEmpty` وبترجع من غير ما تعمل حاجة تحت `flutter test` — يعني
اختبار سلوكي كان هيعدّي سواء الكود موجود ولا لأ.

### ١٣. `lib/core/calc/` ممنوع فيه Flutter imports

قيد صارم — ده اللي بيخلي كل معادلة تتختبر كـ unit test من غير widget binding.

### ١٤. الـ PWA — الزرار مبيظهرش من غير الـ service worker

الموقع بيتثبّت كتطبيق. **`public/sw.js` مش رفاهية أوفلاين** — Chrome مبيطلّعش
حدث `beforeinstallprompt` (اللي زرار «ثبّت التطبيق» في `components/pwa.tsx`
مستنيه) غير لما يكون فيه service worker **مسجّل وفيه `fetch` handler**، بالإضافة
لـ manifest فيه `name`/`short_name`/`start_url`/`display: standalone` وأيقونتين
192 و512. لو واحد ناقص، الزرار مبيظهرش **ومفيش أي رسالة خطأ في أي مكان**.

تلات حاجات لو اتغيّرت تكسر حاجة صامتة:

1. **المستندات network-first، مش cache-first.** `/privacy` و`/terms` مستندات
   قانونية منشورة، وworker يقدّم نسخة قديمة منها لمستخدم أونلاين ده مش تفصيلة.
   الكاش بيتقري بس لما الشبكة تفشل فعلًا.
2. **أي حاجة cross-origin مبتتلمسش.** Firestore وIdentity Toolkit بيعدّوا على
   `fetch` — الـ worker بيرجع من غير `respondWith` خالص، فالمتصفح بيتصرّف كأنه
   مش موجود. صفقة قديمة من الكاش أسوأ بكتير من خطأ أوفلاين.
3. **`cache.put` جوّه `event.waitUntil`.** كتابة مش متنتظرة بتموت لو المتصفح
   وقّف الـ worker بعد `respondWith` — والفشل ده غير مرئي تمامًا: الصفحة شغّالة
   والكاش بس أفضى من المفروض. ده حصل فعلًا واتصلّح.

الأيقونات مولّدة بـ `npm --prefix site run icons` من `assets/logo.png`.
**مش مربوطة بالـ build** بعكس الثيم — الشرح في `site/package.json`.
والـ maskable ملف منفصل عن قصد؛ السبب مشروح في `site/README.md`.

### ١٥. «الهدف» حاجتين مختلفتين — وممنوع تندمج

تبويب «الهدف» فيه سؤالين مش سؤال:

| | بيجاوب على | العائد جاي منين |
|---|---|---|
| **بالتداول** (`goal_projection.dart` / `projection.ts`) | «بأداء دفترك، توصل إمتى» | **متقاس** من الصفقات المقفولة — expectancy × صفقات/شهر |
| **بالادخار** (`goal_plan.dart` / `goal-plan.ts`) | «لو العائد كذا، أحطّ كام شهريًا» | **فرضية بيكتبها المستخدم** |

التانية هي حاسبة الهدف اللي في اللاندينج بيدج، وبقت مشتركة: نفس
`GoalPlannerBody` في الصفحة الرئيسية وفي الداشبورد، ونفس الحساب في التطبيق.

**الرقم المتقاس مبيتحطش في خانة الفرضية لوحده.** لما الدفتر يكون فيه edge،
بيتعرض كـ«من دفترك: X%» بضغطة واحدة — مش كقيمة ابتدائية. حقن قياس مكان فرضية
من غير ما تقول، هو بالظبط اللي `goal_projection` اتكتبت عشان تتجنبه.

**وخانة العائد بتبدأ فاضية في كل مكان، ومفيش preset بيحطّ فيها رقم.** كانت
`20%` افتراضية والـpresets بتاعتها بتعيد كتابتها — يعني اختيار «مستقبل الأبناء»
كان بيدّعي ضمنًا إن البورصة بتدي ٢٠٪ في السنة. ده رقم عائد مستقبلي **مننا**،
واللي `RELEASE.md` و`disclaimer` ماشيين بعيد عنه. الحدود مفروضة في الحساب نفسه:
العائد `0..100` والمدة `1..50` سنة، والسالب بيتعامل كصفر (سايبه بإشارته بيطلّع
قسط شهري **سالب** — خطة بتدفع لصاحبها).

**وشيبس `10% · 15% · 20%` تحت عنوان «افتراضات للتجربة:» — والعنوان ده مش زينة.**
صف نسب من غير عنوان بيتقرا ترشيح؛ نفس الصف تحت الجملة دي بيتقرا اختصارات
للتجربة. (`kReturnSamples` / `RETURN_SAMPLES`، ولازم يفضلوا متطابقين.)

### ١٦. حاسبة الصفقة: كل مستوى بنسبة أو بسعر، والملخص نتايج بس

**الهدف والاستوب الاتنين ليهم مفتاح «نسبة/سعر».** كان الاستوب بس هو اللي عنده،
والهدف نسبة وبس — يعني اللي بيقرا مقاومة من الشارت كان لازم يقسّمها على سعر
دخوله بإيده الأول. `LevelField` في التطبيق و`ToggleField` في الموقع نفس الحاجة،
و`SmartTradePlan.compute` بقى بياخد `targetPrice` زي `stopPrice` بالظبط: السعر
المكتوب بيكسب على النسبة، والنسبة بتتحسب منه للعرض.

**واللي مش مكتوب بيتعرض تحت الخانة نفسها** («= 42.42 ج.م» تحت النسبة، «= 5.0%»
تحت السعر). ودي هي اللي خلّت **الملخص يشيل المستويات كلها**.

**«ملخص الصفقة» فيه اللي اتحسب بس، مش اللي المستخدم كتبه.** كان بيبدأ بسعر
الدخول (مكتوب فوقه بخانتين) وسعر الهدف والاستوب، فالأربع أرقام اللي هي فعلًا
**إجابة** — الكمية وقيمة المركز والربح والخسارة — كانت بتنزل تحت الطية على
التليفون. و«أقصى خسارة مسموحة» بقت سطر صغير جنب المخاطرة اللي بتحدّها، لأنها
`رأس المال × نسبة المخاطرة` من الإعدادات — نفس الرقم في كل صفقة.

**فخ اتمسك في اختبار العرض الضيّق:** سطر المخاطرة كان `Expanded` + أربع عناصر
ثابتة بعده، فطلع بره الكارت ٧٦ بكسل عند 320. أي عنصر ثابت بعد `Expanded` في
`Row` مبيقدرش يصغّر.

### ١٧. معادلة واحدة للحاسبة — كان فيه تلاتة

اللاندينج بيدج والداشبورد **نفس الكومبوننت** (`CalculatorWidget`)، الفرق props
بس. اللي كان مختلف فعلًا هو إن الكومبوننت ده كان بيحسب بنفسه في
`calculator-state.ts` بدل ما ينده الطبقة المشتركة — وده اتشال، وبقى كله على
`smart-trade.ts`.

**والفرق مكانش تجميلي.** الحاسبة كانت بتعمل `Math.floor(budget / riskPerShare)`
inline بدل `suggestedQuantity()` اللي فيها الإبسيلون:

```
دخول 1.10 · استوب 1.00 · رأس مال 10,000 · مخاطرة 1%
raw = 999.9999999999991   →  الموقع 999   ·   التطبيق 1000
```

ده نفس الفلوت اللي `acceptance_test.dart` مثبّت التطبيق عليه. وكمان رقم الـR
كان لونه `text-win` ثابت، فصفقة `0.5R` كانت بتطلع خضرا. **الاتنين اتصلّحوا
بالانتقال للمرآة**، والموقع كسب معاهم خانة المبلغ والحكم على الصفقة.

**وفورم الصفقة الكامل كان تالت معادلة** (`suggestedQuantity` + حساب مخاطرة يدوي
جوّه `useMemo`) — بقى على `computeSizing`.

### ١٨. فورم الصفقة الكامل: نفس ترتيب التطبيق

`trade-form.tsx` مرصوص قسم بقسم زي `trade_form_body.dart`: الحالة · الرمز ·
السعرين · الكمية جنب الهدف · كارت المعاينة · السبب · الخروج · تشيك ليست ·
**فولدر واحد** «أدوات ومرفقات إضافية (صور، تصنيفات، سجل)».

اللي اتشال من الموقع: بانل «حاسبة الحجم» اللي كان فيه رأس المال وسقف المخاطرة
قابلين للتعديل وتحتهم فقرة بتشرح إن التعديل مش بيتحفظ. القاعدة بتتقرا من الحساب
وبتتغيّر من «الإعدادات»، والمقترح بقى helper تحت خانة الكمية زي التطبيق.

**والتاريخ اتضاف للتطبيق بدل ما يتشال من الموقع.** فورم التطبيق مكانش فيه خانة
تاريخ دخول خالص — `DateTime.now()` وقت الحفظ وخلاص — فصفقة اتسجّلت الصبح اللي
بعده تاريخها غلط للأبد، والأداء الشهري ومنحنى رأس المال ومتوسط مدة الاحتفاظ كلهم
بيقروا الحقل ده.

**والفورمين بقوا متطابقين بالكامل** بعد ما `QuoteBadge` اتضاف للموقع — بادج
آخر إغلاق تحت خانة الرمز، مربوط بجدار `livePrices` زي `StockQuoteBadge`.

### ١٩. «مرفوض» و«مش موجود» **مش نفس الحاجة**

مستند الاشتراك بيرجع `null` في الحالتين — حساب جديد لسه مافيهوش مستند، أو
قراءة اترفضت — والاتنين بيتحوّلوا لـ`Entitlement.free`. يعني **نشرة من غير
قواعد بتقفل كل مميزة مدفوعة على كل مستخدم، وبتلغي التجربة، من غير أي رسالة
خطأ**: الرفض متبلوع بـ`catch` عن قصد في الجهتين.

الفرق بقى متقاس صراحةً:

| | التطبيق | الموقع |
|---|---|---|
| الفحص | `FirestoreSyncService.canReadSubscription` | `onSnapshot` error handler |
| الفلاج | `billingReadableProvider` (`bool?`) | `useSubscription().readable` |
| العرض | `PlanCard` — كارت أحمر | قسم أحمر في «الإعدادات» |

**`null` معناها «مش منطبق»** (خارج، أو مفيش Firebase app زي تحت `flutter test`)
— **مش** «مرفوض». خلط الاتنين بيولّع الكارت الأحمر في كل اختبار.

و**الـfallback نفسه لازم يفضل `free`**، مش trial: فتح المدفوع على حساب
مش قادرين نقرا حالته هو الاتجاه الغلط للفشل. اللي اتغيّر إن الـfallback بقى
بيقول عن نفسه بدل ما يتعرض كأنه قرار.

---

### ٢٠. «السوق» ثلاث مصادر، والبورد بيمرّ علينا إحنا الاتنين

تبويب «السوق» فيه تلات حاجات من تلات أماكن مختلفة، وكل واحدة تقدر تغيب من غير
ما تودّي التانيتين معاها:

| الجزء | المصدر | لو وقع |
|---|---|---|
| شريط EGX30 + الأعلى صعودًا | `egxbot.com/live/hero` عن طريق `/api/egxbot-hero` | الشريط بيختفي بالكامل |
| أعلى ٥ / أقل ٥ | TradingView عن طريق `/api/stocks` | رسالة «حدّث» مكان الكروت |
| مين اشترى ومين باع | مجموعة `marketFlows` (إدخال يدوي) | القسم مبيتعرضش أصلًا |

**والتطبيق بيقرا البورد من `/api/stocks`، مش من TradingView مباشرة.** كان بينده
السكانر بنفسه بـ**بودي مختلف** — `type == stock` على ٦٠٠ صف عندنا مقابل
`market == egypt` على ٣٠٠ صف على الموقع. اتقاسوا في ١٨ أغسطس ورجّعوا **نفس
الـ٢٩٢ صف ونفس الخمسة** — يعني الفرق كان **كامن مش ظاهر**، بس اتنين بيسألوا
سؤالين مختلفين وبيتصرّفوا كإجابة واحدة هو بالظبط الشكل اللي بينفجر بعدين.
`TradingViewService._payload` بقى نسخة حرفية من `SCANNER_BODY`، وهو **fallback**
بس لما الراوت مايردّش — نفس النمط بتاع `_quoteEndpoints` في §١٠.

**و`EgxBotHeroWidget` ممنوع يقول «مباشر».** المصدر ده **مبيصرّحش بأي تأخير**
في الرد — بعكس TradingView اللي بيقول `delayed_streaming_900` وإحنا بنكرّر
رقمه. تأخير مش معلن مش معناه صفر تأخير. وكان فيه نقطة خضرا بتنبض جنب الكلمة.

**والبولّينج بيقف لما التاب يبقى مخفي.** كان `setInterval` كل ٣٠ ثانية طول عمر
البانل — داشبورد سايبها مفتوحة في تاب ورا بتفضل تضرب سيرفر حد تاني للأبد. بقى
٦٠ ثانية، و`visibilitychange` بيتخطّى الطلب وهو مخفي وبيحدّث فورًا وهو راجع.

**و`changePercent` بوحدة النسبة في كل حتة** (`2.15` = ‎+2.15%‎)، زي `BoardRow`.
`egxbot-fetch.ts` كان بيخزّن الكسر ويضربه ×١٠٠ في مكان العرض الوحيد — اتفاقين
لنفس الكمية جوّه بانل واحد، وده الشكل اللي بيشحن غلطة ×١٠٠ أول ما تالت قارئ
يقرا الحقل.

**الاستثناء الوحيد `/api/quote`**: هو بيرجّع **كسر** لأن مسار Yahoo بيطلّع كسر،
والراوت بيحوّل نسبة TradingView لكسر عنده — مكتوب في الكود عند التحويل.

**و«الأسهم» بتعرض البورد كله على السطحين.** التطبيق كان بيعرض
`EgxMarketService.egxDirectory` — تلاتين كود مكتوبين في Dart — والموقع كان خلاص
ناقل على `/api/stocks` بكل الـ٢٩٢. نفس التبويب، نفس الاسم، وفرق **عشر مرات**:
حد يلاقي سهم في المتصفح وبعدين يدوّر عليه في التليفون مش هيلاقيه. الدليل لسه
موجود وبيعمل حاجتين: بيدّي **الأسماء العربية** المنسّقة (وصف TradingView إنجليزي
لمعظم الأكواد)، وهو **الاحتياطي** لما البورد مايوصلش عشان البحث يفضل شغّال
أوفلاين بدل ليستة فاضية. `stocks_screen_test.dart` مثبّت التلاتة.

**و`StockRowWidget` بياخد سعره جاهز، مش بيجيبه.** كان بيعمل
`ref.watch(livePriceProvider(code))` لكل صف — بروڤايدر لكل سهم لسعر بيوصل
للـ٢٩٢ كلهم مرة واحدة في نفس رد البورد اللي الشاشة ماسكاه أصلًا.
`livePriceProvider` لسه موجود وصح في `LivePnlView`، اللي فعلًا فيه رمز واحد.

---

### ٢١. حارسان على النص: صفر إيموجي، وممنوع «لحظي»

`test/site_copy_guard_test.dart` بيقرا سورس الموقع والتطبيق ويكسر البناء لو
اتخرقت واحدة من القاعدتين. الاتنين كانوا **مكتوبين هنا بس**، والاتنين اترجعوا:
**١١ إيموجي في ٥ ملفات**، وبادج «مباشر» فوق مصدر مبيصرّحش بتأخير.

- **الإيموجي**: البيكتوجرافات كلها ممنوعة. ✓ و✗ **مسموحين** لأن `title=` نص خام
  ومستحيل تحط فيه SVG — ده تلميح درجة الانضباط.
- **«لحظي»**: ممنوعة إلا لو على نفس السطر نفي (`مش` / `ليست` / `غير`).

> **فخ اتمسك بالحارس نفسه:** أول نسخة كانت `RegExp('(...)\s')`، والـ`\s`
> ضاع في الكتابة فبقى المطلوب حرف `s` بعد كلمة عربية — نمط مبيقابلش أي حاجة،
> وحارس مبيقابلش حاجة بينجح دايمًا. الكلمات بقت `contains` من غير أي escaping،
> و**الحارس اتجرّب بحقن مخالفة فعلية وسقط زي ما المفروض** قبل ما يتسلّم.

---

### ٢٢. الـ CSP بتقفل الشارت — وأي مصدر خارجي جديد لازم يعدّي عليها

`next.config.ts` فيها الـCSP (**مش `vercel.json`** — الملف ده بيرفض التعليقات
فالهيدرز اتنقلت). الشارت بيحقن سكربت من `s3.tradingview.com`، والهوست ده مكانش
في `script-src`، فكل زرار «الشارت» على الموقع النازل **مكان بيعمل حاجة خالص**:

```
blockedURI: https://s3.tradingview.com/.../embed-widget-advanced-chart.js
directive:  script-src-elem
```

والأثر الوحيد سطر في الكونسول محدش بيقراه. وعلى التليفون شغّال عادي (بيفتح
المتصفح الخارجي بـ`url_launcher`)، فالفرق كان بيبان لواحد بيستخدم الاتنين بس.

اتحل بإضافة `s3.tradingview.com` لـ`script-src` و`www.tradingview-widget.com`
(الإطار اللي السكربت بيعمله) لـ`frame-src` — **واتجرّب في متصفح على بناء حقيقي**،
صفر مخالفات والإطار طلع ٦٠٠×٤٠٠ بـcontentWindow شغّال.

> **القاعدة:** أي مصدر خارجي جديد لازم يتحط في الـCSP **وفي سياسة الخصوصية**،
> والاتنين في نفس الكوميت. وسبب الخصوصية إن ده **embed مش relay**: باقي بيانات
> السوق بتخرج من سيرفرنا من غير أي حاجة شخصية، أما الشارت فمتصفح المستخدم هو
> اللي بيكلّم TradingView — يعني IP بتاعه ونوع متصفحه وكوكيزهم. ده تالت طرف
> والسياسة كانت بتقول «حالتين بس».

---

### ٢٣. الصفحات القانونية عليها حارس دلوقتي — والمصدر هو الفلاج

`kEverythingFree` / `EVERYTHING_FREE` هما مصدر الحقيقة، و`site_copy_guard_test.dart`
بيقرا الاتنين ويتأكد إنهم متساويين، وإن **بند ٦ في الشروط مبيبيعش حاجة طول ما
الفلاج `true`**.

اتضاف لأن ده حصل فعلًا: الباقات اتوقفت، واللاندينج بقى بيقول «مجاني بالكامل»
والـstructured data `price: 0` — و**الشروط فضلت شهور بتبيع «رادار Pro»** وبتحوّل
على `/#pricing` اللي مبقاش موجود، وبتقول «ابعتلنا **من داخل رادار** وأول ما تدفع
بنفعّل». والتطبيق بيفتح الصفحة دي من الإعدادات، يعني الجملة اللي اتشالت من
التطبيق عشان Play كانت لسه موصولة من جوّه التطبيق.

الكلمات الممنوعة **لازم تكون كلمات مستحيل تظهر في نفي**. أول نسخة كانت فيها
«تجديد تلقائي» و«بوابة دفع»، فوقعت على النص الجديد نفسه اللي بيقول «ومفيش تجديد
تلقائي» — نفس درس قاعدة «لحظي». والحارس اتجرّب بحقن المخالفتين الحقيقيتين وسقط
في الاتنين قبل ما يتسلّم.

**ولما الباقات ترجع:** الفلاج بيبقى `false`، الاختبار بيتخطّى نفسه، وبند ٦ لازم
يترجع في **نفس الكوميت**. النص القديم في git history عند الكوميت اللي شاله.

### ٢٤. `sizing.ts` مبقاش مرآة — بقى نفس الكود

`lib/core/calc/` **ممنوع فيه Flutter imports** (§١٣)، وده اللي خلّى الطبقة دي
قابلة للنقل من غير ما حد يقصد. وDart بتترجم لـJS. فالموقع بقى **بيشغّل حساب
التطبيق نفسه** بدل ما يقلّده:

```
lib/core/calc/*.dart  ──►  tool/calc_js/calc_api.dart  ──►  dart compile js
                                                              │
                          site/lib/generated/radar-calc.js  ◄─┘  (مكوميت)
                                        ▲
                          site/lib/calc.ts  ──►  site/lib/sizing.ts
```

**خمس حاجات لازم تعرفها:**

1. **المخرج مكوميت، وVercel عمرها ما بتبنيه.** مفيش Dart على سيرفر البناء.
   عشان كده `calc:check` **بيهاش المصادر** (SHA-256 على `lib/core/calc/**` +
   الجسر) ويقارنها بالبصمة المكتوبة في هيدر الملف المولّد — بدل ما يعيد
   الترجمة زي `theme:check`. يعني بيشتغل في أي مكان فيه Node.
2. **الحارس اتجرّب بحقن مخالفة فعلية**: تغيير `kQtyEpsilon` من `1e-9` لـ`1e-6`
   خلّى `calc:check` يسقط بكود `1`، ورجوعه خلّاه يعدّي.
3. **الجسر JSON داخل وJSON خارج.** البديل إنك تعمل موديل لكل كلاس Dart في JS
   — نسخة تانية من نفس المشكلة. التكلفة متقاسة: **3.8 ميكروثانية** لنداء
   `sizing` (اللي بيتحسب مع كل ضغطة زرار)، و**6.6 ms** لتحليلات دفتر ٧٠٠ صفقة.
4. **الهجرة اتحققت مش اتفرضت.** النسخة القديمة والمترجمة اتشغّلوا على **٤٥٬١٠٦
   مدخل** — صفر وسالب و`1e-9` و`1e12` و`±Infinity` و`NaN` و`null` و`undefined`
   لكل بارامتر، وكنس عشوائي ٤٠ ألف حالة — واتفقوا على الـ١١ حقل في كلهم،
   بمقارنة `Object.is` عشان `-0` و`NaN` ما يعدّوش كأنهم مساويين لحاجة تانية.
   و`tool/calc_js/verify.dart` + `verify.mjs` بيشغّلوا الـ١٣ دالة على الـVM وعلى
   المترجم ويقارنوا **٤١٠ قيمة** — دي الحارس الدائم إن الحزمة بتقول نفس كلام
   الـDart.
5. **الباقي لسه مرايا.** `sizing.ts` هي اللي اتنقلت. الباقيين بيتنقلوا على
   **نفس الحزمة** فمش هيزوّدوا حجم — الحزمة كلها **26.3 KB brotli** وفيها
   الـ١٣ دالة أصلًا. لحد ما يتنقلوا، قاعدة «غيّرها في الاتنين» لسه سارية عليهم.

**ولو غيّرت أي معادلة في Dart:** `npm --prefix site run calc` وكوميت المخرج.
نسيت؟ البناء بيقع عند `calc:check` — مش بيعدّي في صمت.

### ٢٥. Bloc/Cubit وFirestore — اتعملت في ٢٨ أغسطس

قرار المالك (٢٧ أغسطس)، واتنفّذ بالكامل في اليوم اللي بعده. **صفر Riverpod،
صفر Hive**، والحزمتين اتشالوا من `pubspec.yaml`.

#### اللي اتشال واللي دخل مكانه

| كان | بقى |
|---|---|
| ٢٧ بروڤايدر Riverpod في ٤٨ ملف | **١٠ كيوبتس** |
| ٥ بوكسات Hive (`trades` · `watchlist` · `settings` · `auth` · `syncState`) | **Firestore** لبيانات المستخدم · **SharedPreferences** لتفضيلات الجهاز |
| `sync_merge.dart` + `sync_provider.dart` + ٣ adapters + `hive_keys.dart` | **اتشالوا** — شوف §٩ |
| `AuthRepository` على بوكس `auth` | `AuthCubit` على `authStateChanges()` |
| ٥٢٨ اختبار، كتير منهم بيفتح بوكسات Hive حقيقية | **٥٣١**، وكلهم على `fake_cloud_firestore` عن طريق `test/support/app_harness.dart` |

الكيوبتس العشرة: `TradesCubit` · `WatchlistCubit` · `SettingsCubit` ·
`AuthCubit` · `BillingCubit` · `MarketCubit` · `DevicePrefsCubit` ·
`ShellCubit` (+ `AccountScopedCubit` الأساس، و`AppHarness` في الاختبارات).

#### الحاجات التلاتة اللي «أونلاين» مبيحلهاش — والقرار اللي اتاخد فيها

القرار: **بيانات المستخدم أونلاين وبس** (الصفقات، قائمة المراقبة، إعدادات
المخاطرة). **تفضيلات الجهاز تفضل على الجهاز** في `SharedPreferences`:

1. **مفتاح Gemini** — §١٠ وسياسة الخصوصية المنشورة الاتنين بيقولوا إنه مبيتحطش
   في Firestore. اعتماد طرف تالت بيتحاسب عليه المستخدم؛ حاجة مبتخرجش من الجهاز
   مبتتكشفش بغلطة في قواعدنا ولا في لوحة الإدارة. **رفعه أونلاين هو تغيير في
   مستند قانوني منشور، مش تفصيلة تقنية.**
2. **الثيم وعلامة الأونبوردنج** — تفضيلات جهاز مش بيانات مستخدم. قراءة الثيم من
   الشبكة معناها نداء **قبل أول رسم**: ومضة بيضا على كل فتح، للأبد.
3. **كاش Firestore المحلي** شغّال (افتراضي على الموبايل) و**مش مخزن تاني** —
   شوف §٩.

**والعادتين معاهم**: `enableChecklist` و`enableConfirmations` على الجهاز، لأن
مزامنة عادة بتفرض تفضيل جهاز على جهاز تاني — والكوديك كان مسجّل السبب ده أصلاً.

#### تلات حاجات اتغيّرت في السلوك، ومقصودة

- **`Firebase.initializeApp()` بقى إجباري.** كان الفشل بيتبلع «عشان الدفتر
  يفتح أوفلاين» — مفيش دفتر محلي دلوقتي، فالفشل بيوري `StartupFailureApp` بدل
  ما يسيب المستخدم في تطبيق كل شاشة فيه فاضية من غير سبب معلن.
- **`AuthGate` بقى ليه حالة تالتة.** Firebase بيسترجع الجلسة **بشكل غير
  متزامن**، فاعتبار «لسه مش عارفين» = «مش مسجّل» كان هيوري فورم دخول لواحد
  مسجّل من شهور، كل مرة يفتح.
- **خانة «امسح كمان من التليفون» في حذف الحساب اتشالت.** كانت سؤال حقيقي لما
  كان فيه نسختين؛ دلوقتي هي بتسأل عن حاجة مش موجودة، والأسوأ إنها بتوحي إن فيه
  نسخة بتنجو من الحذف.

## حاجات اتقررت وممنوع ترجع من غير سبب

| القرار | السبب |
|---|---|
| تسجيل دخول **إجباري** | قرار المالك (29 يوليو) |
| ~~**Hive باقي**~~ **اتعكس ٢٧ أغسطس · اتنفّذ ٢٨** | قرار المالك: **مفيش Hive، وكل حاجة بتتسجّل أونلاين**. اتشال بالكامل. شوف §٩ و§٢٥ |
| **الحالة بـBloc/Cubit مش Riverpod** | قرار المالك (٢٧ أغسطس)، **واتنفّذ ٢٨ أغسطس** — صفر بروڤايدرز، والحزمة اتشالت من `pubspec.yaml`. شوف §٢٥ |
| **عربي بس — الإنجليزي اتشال من الخطة** | قرار المالك (٢٧ أغسطس) بعد ما اتحسبت التكلفة: ~٧٥٥ نص في التطبيق، ٩٨ ملف في الموقع، ونسخة تانية من المستندات القانونية. الجمهور كله بيتكلم عربي |
| **مفيش أدمن تاني** | الشريك **مش تقني ومش بيشتغل على التطبيق** (٢٧ أغسطس). مجموعة `admins` بتفضل فيها حساب واحد، ونموذج الأدمن ما اتلمسش |
| ~~مفيش باقات مدفوعة~~ **اتغيّر ٦ أغسطس** | المالك ضاف Radar Pro (٩٩ شهري · ٤٩٩ نصف سنوي · ٧٩٩ سنوي) + ١٤ يوم تجربة. الدفتر والحاسبات مجانية للأبد |
| **مفيش ترشيحات أسهم — لا مدفوعة ولا مجانية** | تعارض مع `RELEASE.md` وخطر ترخيص FRA. النسخة المدفوعة اتشالت من الخطة من الأول، والمجانية (مجموعة `signals` وتبويب «الصفقات» في الأدمن) اتشالت بالكامل في ٥ أغسطس ٢٦ — كانت بتناقض التنويه والشروط والـ FAQ في نفس الوقت. القواعد بتمنعها دلوقتي |
| `playStoreUrl: null` | التطبيق مانزلش. كل أزرار التحميل بتتحوّل لـ«قريبًا» لوحدها |
| `userCount: null` | مفيش مستخدمين. السطر مختفي بالكامل لحد ما يبقى فيه رقم حقيقي |
| **صفر إيموجي** في الموقع | تفضيل صريح من المالك — أيقونات SVG بس (`site/components/icons.tsx`) |

---

## الخطوات المعلّقة على المالك

### مفيش حاجة بتوقف الإطلاق دلوقتي — والإطلاق نفسه مؤجّل

**قواعد Firestore اتنشرت** (٢٧ أغسطس، تأكيد المالك). البند ده كان بيقول إن
كل حساب هيتقرا مجاني والتجربة عمرها ما هتبدأ في صمت — خلاص اتقفل.

**والمالك قرر إن التطبيق مش هيترفع على Play قريبًا** (٢٧ أغسطس). البندين
التحت دول **لسه لازمين، بس مش دلوقتي** — سيبهم مكتوبين لأن نسيانهم يوم الرفع
بيكسر الدخول بجوجل، ومحدش هيفتكرهم بعد شهور.

**١. SHA-1 المسجّل في Firebase هو بصمة مفتاح الديباگ — مش مفتاح رفع.**
اتقاس بالكود في ٢٦ أغسطس، والنتيجة عكس اللي كان مكتوب هنا قبل كده:

```
debug.keystore SHA1  : 15:68:66:D5:A6:74:9D:7E:26:22:8B:78:00:38:98:72:05:DB:EF:82
google-services.json : 156866d5a6749d7e26228b780038987205dbef82      ← نفسها بالظبط
apksigner على APK    : CN=Android Debug
```

يعني الدخول بجوجل شغّال دلوقتي **لأن مفتاح الديباگ هو المسجّل**، وأول ما
تعمل مفتاح رفع حقيقي الـAPK هيتوقّع بمفتاح تاني والدخول **هيقع** — إلا لو
ضفت بصمته.

**محتاج تضيف بصمتين في Firebase، مش واحدة:**

1. **مفتاح الرفع** — بعد ما تعمل الـkeystore: `cd android && ./gradlew signingReport`
2. **Play App Signing** — بعد أول رفع، من Play Console → Setup → App signing.
   جوجل بتعيد توقيع الحزمة بمفتاحها هي، فالبصمة اللي التليفونات هتشوفها **مش**
   بتاعة مفتاح الرفع.

وبصمة الديباگ ممكن تسيبها للتطوير المحلي، بس هي مالهاش أي دور في نسخة المتجر.

**٢. `android/key.properties` مش موجود.** البناء دلوقتي بيقع على **مفتاح
الديباگ** وPlay بيرفضه. `RELEASE.md` قسم ١.

> **والتحذير اللي المفروض ينبّهك مش بيظهر.** `build.gradle.kts` فيه
> `logger.warn(...)` لما الملف ما يكونش موجود، بس `flutter build apk --release`
> **مبيطلّعش السطر ده خالص** — اتجرّب في ٢٦ أغسطس وطلع بناء ناجح ٦٠ ميجا من
> غير أي كلمة عن التوقيع. الطريقة الوحيدة إنك تعرف هي إنك تسأل الـAPK نفسه:
>
> ```bash
> $ANDROID_HOME/build-tools/<ver>/apksigner verify --print-certs
>     build/app/outputs/flutter-apk/app-release.apk
> ```
>
> لو طلع `CN=Android Debug` يبقى لسه بيتوقّع بالديباگ.

### بعد الإطلاق مباشرةً

1. **`playStoreUrl` في `site/lib/site.ts` لسه `null`** — كل أزرار التحميل
   بتقول «قريبًا». يوم ما التطبيق ينزل، غيّره وإلا الموقع هيفضل يقول قريبًا
   والتطبيق على المتجر.
2. **اختار دومين حقيقي.** الموقع على `.vercel.app`، وده اللي كل رابط أساسي
   وخريطة الموقع وصورة المشاركة بيشاوروا عليه. لما تشتري دومين: ضيفه في
   Vercel → Domains، غيّر `NEXT_PUBLIC_SITE_URL`، وأعد النشر — **وضيفه كمان
   في Firebase Console → Authentication → Settings → Authorized domains**،
   وإلا تسجيل الدخول بيرجع `auth/unauthorized-domain`. **وغيّر `kSiteUrl` في
   `lib/settings/widgets/legal_tiles.dart`** — ده الرابط اللي التطبيق بيفتح
   بيه السياسة والشروط، ومحطوط بإيده لأن مفيش بيئة بناء على التليفون.
3. **وفي نفس اليوم: نقل `authDomain` للدومين بتاعك.** ده مش تجميل — ده اللي
   بيقتل مشكلة الدخول بجوجل من جذرها. اعملها يوم الدومين عشان ما تتعملش
   مرتين. التفاصيل تحت.

#### نقل مُعالج الدخول لدومينك — الخطوات والفخّين

**المشكلة اللي بيحلها:** الدخول بجوجل بيمشي على
`trademind-6222c.firebaseapp.com` — دومين تاني غير دومينك. ده بيخلّق طريقين
كل واحد فيهم بيقع لسبب مختلف:

| الطريق | بيقع لما |
|---|---|
| `signInWithPopup` | المتصفح يمنع النوافذ · التطبيق متثبّت كـPWA · متصفح جوّه تطبيق تاني |
| `signInWithRedirect` | المتصفح يقفل تخزين الأطراف التالتة (Chrome ماشي في الاتجاه ده) |

الكود بيجرّب النافذة الأول وبيقع على التحويل لو اتمنعت (`auth-context.tsx`)،
فمفيش طريق مسدود دلوقتي. **بس الاتنين لسه بيعدّوا على دومين تاني.** خلّي
المُعالج على دومينك، والسببين الاتنين يختفوا — بقى كله same-origin.

**الخطوات:**

1. `next.config.ts` → `rewrites()`:
   `{ source: '/__/auth/:path*', destination: 'https://trademind-6222c.firebaseapp.com/__/auth/:path*' }`
2. `site/lib/firebase.ts` → `authDomain` يبقى دومينك بدل `*.firebaseapp.com`.
3. الدومين لازم يكون في Firebase → Authorized domains (نفس البند فوق).

**والفخّين — الاتنين بيفشلوا في صمت، ومحدش هيربطهم بالتغيير ده:**

- **`frame-ancestors 'none'`.** `signInWithPopup` بيركّب iframe على
  `authDomain/__/auth/iframe` **جوّه صفحتنا**. طول ما الـauthDomain برّه،
  الـCSP بتاعتنا مالهاش دعوة بيه. أول ما يبقى على دوميـنا، الهيدر بتاعنا
  بيتحط على الـiframe نفسه و`frame-ancestors 'none'` **بيمنع صفحتنا من
  تضمينه**.
- **`form-action 'none'`.** مُعالج الدخول بيعمل form submit لجوجل. دلوقتي
  الـCSP بتاعتنا مش واصلاه؛ بعد النقل بتوصله وبتمنعه.

**الحل:** بلوك هيدرز منفصل لـ`/__/auth/:path*` في `next.config.ts` من غير
الـCSP بتاعتنا — الصفحة دي بتاعة جوجل، وسياستنا مكتوبة لصفحاتنا إحنا.

**واتأكد بمتصفح على بناء إنتاج، مش بالتحليل.** ده نفس درس §٢٢ بالظبط: الـCSP
قفلت الشارت وما ساب غير سطر في الكونسول محدش بيقراه. ولازم تكمّل تسجيل دخول
حقيقي للآخر — رحلة الرجوع هي اللي فيها الفخّين، والرايح بيعدّي عادي.
**اتعمل خلاص:** حساب الأدمن اتعمل · الموقع نازل على Vercel · **الوعد بتنبيه
داخل التطبيق اتشال من بند ٩** (٢٦ أغسطس) — كان بيوعد بحاجة السطح بتاعها
(«المستجدات») اتشال بكوده، فالبند بقى بيوعد بتحديث التاريخ بس، زي بند ٧ في
الشروط بالظبط. لو اتبنى سطح تنبيه بعدين، البند يتوسّع ساعتها **وساعتها بس**.

### أربع أسئلة مفتوحة — اتسألت في ١١ أغسطس ولسه من غير جواب

قرارات المالك، مش شغل مؤجّل. **اسأله عليها قبل ما تشتغل في أي حاجة بتلمسها.**

1. **Play Billing.** على أندرويد أي اشتراك رقمي بيتباع جوه التطبيق لازم يعدّي
   على Play Billing. دلوقتي التطبيق مفيهوش زرار شراء وبيقول «الاشتراك بيتم
   يدوي، ابعتلنا» — ده لسه ممكن يتقرا كتوجيه لدفع خارجي وياخد رفض. الاختيار
   الأأمن: **يشيل أي ذكر للاشتراك من التطبيق** ويسيب البيع على الويب بالكامل.
2. **«السوق» ميزة مدفوعة ومصدرها مقفول** بـF5، والبيانات بتتدخّل بإيد المالك
   ١٢ خانة كل جلسة. لو نسي يوم، اللي دفع بيلاقيها فاضية. يفضل مدفوع بالتزام
   يومي، ولا يترفع من الباقة لحد ما يبقى فيه مصدر آلي؟
3. **الدومين قبل أو بعد نشر التطبيق؟** `kSiteUrl` في
   `lib/settings/widgets/legal_tiles.dart` بيتشحن **جوه الـAPK**، فنشر قبل
   شراء الدومين معناه نسخة جديدة من التطبيق بعدها عشان الروابط تتغيّر.
4. **صور الشارت وخطة Blaze.** الميزة بتتحسب ٢٠ نقطة في درجة الانضباط وما
   ينفعش تتعمل من المتصفح. تفضل مؤجّلة (والواجهة بتقول كده)، ولا تتشال من
   الدرجة عشان محدش يخسر نقط في حاجة مش قادر يعملها؟

### النشر

**مفيش أمر.** المستودع موصول بمشروع Vercel `radar`، فأي وصول لـ`main` بينشر
لوحده.

**بس `main` مقفولة على الدفع المباشر** (٢٧ أغسطس، قرار المالك). الشغل بيروح
على فرع وبعدين Pull Request:

```bash
git switch -c fix/<اسم-مختصر> && git push -u origin HEAD && gh pr create --fill
```

حارسين، والترتيب بينهم مقصود:

- **GitHub branch protection على `main`** — الحارس الحقيقي، مفروض على السيرفر
  و`enforce_admins` مفعّلة، يعني بتنطبق على المالك كمان. من غيرها الحماية
  بتبقى نصيحة.
- **`tool/hooks/pre-push`** — حارس محلي **سريع** بيوقف الدفع قبل ما يخرج من
  الجهاز برسالة بتقول تعمل إيه، بدل ما تستنى رفع كامل وترفض من بعيد. مفعّل
  بـ`git config core.hooksPath tool/hooks` — **لازم تتعاد في أي نسخة جديدة من
  المستودع**، لأن إعدادات git مش بتتكوميت.

والاتنين اتجرّبوا: الدفع على `main` بيترفض، والدفع على فرع بيعدّي.

**وVercel بتبني preview لكل فرع**، فبتشوف التغيير شغّال على رابط حقيقي قبل ما
يوصل للإنتاج — الدمج على `main` هو اللي بينشر.

تلات إعدادات على المشروع هما اللي بيخلّوه يشتغل، ولو واحد اتغيّر البناء بيقع:

| الإعداد | القيمة | ليه |
|---|---|---|
| Root Directory | `site` | من غيرها هيدوّر على `package.json` في الجذر ويلاقي مشروع Flutter |
| **Include source files outside the Root Directory** | **مفعّلة** | `npm run build` بيبدأ بـ`theme:check`، وده بيقرا `design/palettes.json` ويقارن بـ`lib/core/theme/palettes/generated_palettes.dart` — الاتنين بره `site/`. تقفلها، البناء يقع عند `readFileSync` |
| `NEXT_PUBLIC_SITE_URL` | `https://radar-one-phi.vercel.app` | canonical والسايت ماب وصورة المشاركة |

للنشر بإيدك (لو عايز تتخطّى Git):

```bash
npx vercel deploy --prod    # من جذر الريبو، مش من site/ — لنفس سبب الجدول فوق
```

و`--prebuilt` **مبيشتغلش من ويندوز**: البناء بيطلّع symlinks في
`.vercel/output/functions/*.segments/` والرافع مش بيقدر يحلّها، فبيقع بـ
`ENOENT ... __PAGE__.segment.rsc.func`. سيب Vercel يبني على سيرفره.

قواعد Firestore لسه بتتنشر بإيدك، وده الاستخدام الوحيد الباقي لـ`firebase.json`:

```bash
firebase deploy --only firestore:rules
```

---

## حاجات معروفة وناقصة

- **Flutter بيتثبّت في الجلسة دي.** الشبكة بتسمح بـ`storage.googleapis.com`،
  فـ`flutter analyze` و`flutter test` **بيشتغلوا فعلًا** دلوقتي:
  ```bash
  curl -sSL -o /tmp/f.tar.xz "https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.8-stable.tar.xz"
  tar xf /tmp/f.tar.xz -C /opt && git config --global --add safe.directory /opt/flutter
  export PATH="/opt/flutter/bin:$PATH"
  ```
  لازم **3.44.8** أو أحدث — الإصدارات الأقدم عندها Dart 3.5 والمشروع طالب ^3.12.2.
- **الأسعار آلية، وتعاملات المستثمرين لأ — والفرق ده مهم.** الأسعار بتيجي
  لوحدها من TradingView عن طريق `/api/stocks`، ومحدش بيلمسها. اللي لسه يدوي
  هو **جدول «مين اشترى ومين باع»** وبس، والسبب مش الكود: `egx.com.eg` وراه
  **F5 Shape Bot Defense** (الرد بيرجع `window["bobcmn"]` وكوكيز `TSPD_101`)،
  وده منتج تجاري بيطلب تنفيذ JavaScript مشفّر في متصفح حقيقي. اتجرّب من
  الجلسة دي (بيرجع 403) ومن Vercel (بيرجع 200 بصفحة تحدي). **مفيش هيدرز ولا
  كوكيز هيعدّوا منه** — ده الغرض منه أصلًا. `/api/egx-flows` سايب زي ما هو
  بيفشل بصوت عالي وبيرجّع عيّنة من الرد، والطريق الشغّال هو **الإدخال اليدوي**
  في لوحة الإدارة (`manual-flows-form.tsx` — ١٢ خانة بس، و«الكل» و«الصافي»
  بيتحسبوا). البدائل الجدية: مزوّد بيانات مدفوع، أو ترخيص بيانات من البورصة.
- **الشبكة الخارجية بقت مفتوحة** (اتقاست في ١١ أغسطس): `google.com` بيرجّع 200
  و`query1.finance.yahoo.com` بيرجّع 429 — يعني الطلب وصل فعلًا واترفض بالمعدّل
  مش بالـproxy. `egx.com.eg` لسه مش بيرد (F5). الملاحظة القديمة كانت بتقول إن
  كل حاجة مقفولة، وده **مابقاش صح** — `flutter pub add` وتنزيل الحزم شغّالين.
- **Chromium وPlaywright متوفرين، واستخدمهم.** ده اللي مسك الباگات اللي التحليل
  الساكن مبيشوفهاش — التمدد الأفقي، والـ`999` مقابل `1000` في المتصفح:
  ```bash
  npm --prefix site run build && npm --prefix site run start   # مش next dev
  cd /tmp && npm i playwright --no-save
  # chromium: executablePath: '/opt/pw-browsers/chromium'
  ```
  **افحص عند 320 و1280**، وقيس `scrollWidth - clientWidth` مش بالعين بس.
- **صور الشارت مش بترفع.** بتتقرا من Firestore وبتتحسب في درجة الانضباط، بس
  **مش بتتعرض ولا بتتضاف** من المتصفح — دي مسارات مطلقة جوه ذاكرة التليفون.
  الواجهة بقت **بتقول ده صراحةً** في فورم الصفقة وفي تلميح درجة الانضباط، بدل
  ما تسيب المستخدم يشوف ٨٠/١٠٠ من غير سبب. الحل الكامل محتاج Firebase Storage
  (يعني خطة Blaze) ورفع من التطبيق — **اتأجّل بقرار المالك قبل الإطلاق**.
- **مزامنة الإعدادات آخر-كتابة-تكسب.** `users/{uid}/settings/risk` مالوش طابع
  زمني ولا دمج: التليفون بياخد نسخة الحساب أول ما يسجّل دخول، والويب بيقراها
  أول ما يفتح، وأي حفظ بيستبدل المستند كله. بيتلاقوا دايمًا، بس مش بالضرورة على
  آخر تعديل لو الاتنين اتعدّلوا أوفلاين. مقصود — طابع زمني من ساعة العميل بيدخّل
  عطل أسوأ (تليفون ساعته غلط بيثبّت إعداداته للأبد).
- **صورة المشاركة (OG)**: satori مش بيظبط ترتيب الكلمات العربية، فكل كلمة
  اتعملت flex item لوحدها بهوامش معايرة بالعين في
  `site/app/og/route.tsx`. **لو غيّرت أي نص أو مقاس خط هناك، افحص الـ PNG
  الناتج بعينك** — مش هيظبط لوحده.

  **والمسار `/og/` مش `app/opengraph-image.tsx` عن قصد.** اتفاقية الملف بتاعة
  Next بتطلّع رابط الصورة **من غير سلاش أخير** بينما `trailingSlash: true`
  شغّالة، فـ`og:image` كان بيرد **308** على كل سكرابر. اللي بيتبع التحويل شاف
  الكارت، **وواتساب مبيتبعوش فمشافش أي صورة** — والـPNG نفسه كان سليم طول
  الوقت، فالبناء بينجح والباگ مبيبانش غير في شاشة محادثة. الرابط بقى مكتوب
  صراحةً في `app/layout.tsx`، والمولّد بقى راوت عادي لأن **اتفاقية الملف
  بتكسب على الميتاداتا** لو الاتنين موجودين — يعني رجوع `opengraph-image.tsx`
  بيرجّع الباگ وهو ساكت.

  والرابط دلوقتي ثابت من غير بصمة، فكاش السكرابرز بيمسك عليه: لو الكارت
  اتغيّر، الصورة القديمة بتفضل تبان في المحادثات القديمة. أعد السحب من
  أدوات المنصة، أو زوّد `?v=2` على الرابط في `layout.tsx`.

---

## أوامر

```bash
flutter analyze && flutter test          # التطبيق — لازم الاتنين يعدّوا
npm --prefix site run build              # الموقع (بيتحقق من الثيم الأول)
cd site && npx tsc --noEmit              # أسرع من build وقت التعديل
npm --prefix site run theme              # ولّد الألوان بعد تعديل palettes.json
node tool/gen-theme.mjs --check          # اتأكد إن المولّد متطابق
npm --prefix site run calc               # أعد ترجمة طبقة الحساب لـJS (محتاج Dart)
node tool/gen-calc-js.mjs --check        # اتأكد إن الحزمة مطابقة للـDart (مش محتاج Dart)
dart run tool/calc_js/verify.dart        # نفس الدوال على الـVM — للمقارنة
npm --prefix site run icons              # ولّد أيقونات الـ PWA بعد تغيير اللوجو
```

لفحص الـ PWA لازم بناء إنتاج — `next dev` مبيسجّلش الـ service worker عن قصد:

```bash
npm --prefix site run build && npm --prefix site run start
```

خطوات الفحص في DevTools بالتفصيل في `site/README.md`.

---

## أسلوب الشغل المتوقّع

- **اتحقق قبل ما تدّعي.** الشغل اللي فات كله اتفحص فعليًا — لقطات من مخرجات
  **الإنتاج** (مش سيرفر التطوير، اللي كان بيقدّم CSS قديم مرة)، وفحص تمدد أفقي
  عند 320 و1280، وتشغيل الحاسبة من الأول للآخر.
- **متكتبش أرقام في التسويق من دماغك.** كل رقم على الصفحة اتعدّ من الكود.
- **لو حاجة هتناقض الصفحات القانونية، قول قبل ما تعملها.**
- المالك بيتكلم عربي مصري ويفضّل الرد بيه.
